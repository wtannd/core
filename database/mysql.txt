-- ------------------------------------------------------------------------------------------------------
-- Initial mysql script for creating the database for the Community-driven Open Research Ecosystem (CORE)
-- ------------------------------------------------------------------------------------------------------

-- Settings and database creation

SET sql_mode='NO_AUTO_VALUE_ON_ZERO';
SET time_zone = '+00:00';    -- set time zone to UTC
SET NAMES utf8mb4;

DROP DATABASE IF EXISTS `core`;
CREATE DATABASE IF NOT EXISTS `core` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
USE `core`;


-- Global parameters in CORE

CREATE TABLE `IntParameters` (  -- valid: the parameter is in effect starting from this time; current and historical logs
    pname VARCHAR(255) NOT NULL,
    pvalue INT UNSIGNED NOT NULL,
    valid DATETIME NOT NULL default '0000-00-00 00:00:00',  -- starting valid time
    ts DATETIME NOT NULL default CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,  -- update time
    is_current TINYINT(1) NOT NULL DEFAULT 1,  -- 1=the current parameter, 0=obsolete
    PRIMARY KEY (pname, valid),
    INDEX (pname,is_current)
);
-- initial int parameters
INSERT INTO IntParameters (pname,pvalue) VALUES ('max_dutysum',875); -- maximum of sum of all duty values
INSERT INTO IntParameters (pname,pvalue) VALUES ('nthr_rate',5);  -- threshold number of ratings to reveal ECP
INSERT INTO IntParameters (pname,pvalue) VALUES ('nthr_invited',6);  -- Nthreshold for invited reviews
INSERT INTO IntParameters (pname,pvalue) VALUES ('nthr_contributed',10);  -- Nthreshold for contributed reviews
INSERT INTO IntParameters (pname,pvalue) VALUES ('nthr_comment',50);   -- Nthreshold for comments
INSERT INTO IntParameters (pname,pvalue) VALUES ('AL_max',5);  -- maximum number of achievements to be considered
INSERT INTO IntParameters (pname,pvalue) VALUES ('max_adoc',64);  -- maximum number of r-docs allowed in one a-doc
-- update the latest one: UPDATE IntParameters SET pvalue = new_value WHERE pname = xxx AND is_current=1
-- insert a new one: INSERT INTO IntParameters (pname,pvalue,valid) VALUES ('new_name',new_value,NOW()); and set the old one is_current=0

CREATE TABLE DoubleParameters (  -- valid: the parameter is in effect starting from this time; current and historical logs
    pname VARCHAR(255) NOT NULL,
    pvalue DOUBLE NOT NULL,
    valid DATETIME NOT NULL default '0000-00-00 00:00:00',  -- starting valid time
    ts DATETIME NOT NULL default CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,  -- update time
    is_current TINYINT(1) NOT NULL DEFAULT 1,  -- 1=the current parameter, 0=obsolete
    PRIMARY KEY (pname, valid),
    INDEX (pname,is_current)
);
-- initial double parameters
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('a',18e0);
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('b',1.6);
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('c',1.5);
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('d',0.888);
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('a_inv',0.6);  -- a value for ECP of invited review
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('a_con',0.4); -- a value for ECP of contributed review
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('ECP_vote',1.0); -- voting in one v-doc worth f_vote points for ECP
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('fo_1',0.5);   -- fo openness factor for semi-anonymous
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('fo_2',0.25); -- fo openness factor for anonymous
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('Sp0',2);  -- <S>_p0 in attention factor f_a
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('f_neg',3); -- 3 for negative case in f_a and also in ALS
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('AL_base',6);
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('AL_quality',2.5);
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('AL_k',6);  -- the exponent k=6 used in a-doc synthesized Asyn calculations
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('feb_1',0.5); -- factor in f_eb
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('ft_1',0.125); -- factor in f_t
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('fr',2);  -- rating factor for a parent doc
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('fd_1',0.4); -- factor in f_d (rating with invited review)
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('fd_2',0.2); -- factor in f_d (rating with contributed review)
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('fd_3',0.1); -- factor in f_d (rating with creditable comments)
INSERT INTO DoubleParameters (pname,pvalue) VALUES ('fd_4',0.01); -- factor in f_d (rating with no creditable contributions)


-- Research branches and their parameters in CORE

CREATE TABLE BranchTypes (   -- in the format of Discipline.FIELD.area.Direction_Specific
    ID TINYINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    btname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
--    is_active TINYINT(1) NOT NULL default 1,  -- 0 means a decommissioned type
    detail TEXT COLLATE utf8mb4_general_ci
);
INSERT INTO BranchTypes (ID,btname,detail) VALUES (1,'Discipline','top level branches; format: TitleCase; special: Cross, All');
INSERT INTO BranchTypes (ID,btname,detail) VALUES (2,'Field','2nd level branches; format: ALLCAPS or ACRONYM; special: MISC');
INSERT INTO BranchTypes (ID,btname,detail) VALUES (3,'Area','3rd level branches; format: all-lowercases; special: other');
INSERT INTO BranchTypes (ID,btname,detail) VALUES (4,'Direction','4th level branches, format: Title_Case; no special one, all direction have to be specific');
-- Topics instroduced below when multiple branches are significantly involved may have multiple parents instead of one.

CREATE TABLE ResearchBranches (  -- for traditional way of linear single-parent research subjects 
    bID INT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    parent_bID INT UNSIGNED,        -- For hierarchical structure (NULL for disciplines)
    btype TINYINT UNSIGNED NOT NULL,     -- 1='Discipline', 2='Field', 3='Area', 4='Direction'
    bname VARCHAR(255) COLLATE ascii_general_ci UNIQUE NOT NULL,  -- e.g., 'Physics', 'Nuclear Physics', 'Nuclear Experiment'
    abbr VARCHAR(30) COLLATE ascii_general_ci UNIQUE NOT NULL,    -- e.g., 'Phys', 'Phys.NP', 'Phys.NP.nucl-ex'
--    f_n FLOAT NOT NULL default 1,     -- the normalization factor for ECP from both parent documents and derived contributions that accounts for differences in different branches
    is_active TINYINT(1) NOT NULL default 1,    -- 0 means a decommissioned research branch
    detail TEXT COLLATE utf8mb4_general_ci,     -- Optional detailed description
    -- ... metadata about the branch (e.g., key journals, representative topics)
    FOREIGN KEY (btype) REFERENCES BranchTypes (ID),
    FOREIGN KEY (parent_bID) REFERENCES ResearchBranches (bID), -- Self-referencing for hierarchy
    INDEX (parent_bID, abbr, is_active),
    INDEX (btype, abbr, is_active)                       -- Index to filter by branch type
);
-- Disciplines
INSERT INTO ResearchBranches (bID, btype, bname, abbr) VALUES (1,1, 'All Disciplines', 'All');  -- a dummy representing all subjects; no sub-branches should be allowed
INSERT INTO ResearchBranches (btype, bname, abbr) VALUES (1, 'Physics', 'Phys');
INSERT INTO ResearchBranches (btype, bname, abbr) VALUES (1, 'Mathematics', 'Math');
INSERT INTO ResearchBranches (btype, bname, abbr) VALUES (1, 'Chemistry', 'Chem');
INSERT INTO ResearchBranches (btype, bname, abbr) VALUES (1, 'Life Science', 'Life');
INSERT INTO ResearchBranches (btype, bname, abbr) VALUES (1, 'Medical Science', 'Med');
INSERT INTO ResearchBranches (btype, bname, abbr) VALUES (1, 'Earth Science', 'Earth');
INSERT INTO ResearchBranches (btype, bname, abbr) VALUES (1, 'Computer Science', 'Comp');
INSERT INTO ResearchBranches (btype, bname, abbr) VALUES (1, 'Engineering and Technology', 'Engr');
INSERT INTO ResearchBranches (btype, bname, abbr) VALUES (1, 'Social Science', 'Soc');
INSERT INTO ResearchBranches (btype, bname, abbr) VALUES (1, 'Uncategorized', 'Uncat');  -- to be categorized in future, new directions
INSERT INTO ResearchBranches (btype, bname, abbr) VALUES (1, 'Cross-Disciplinary', 'Cross');
-- INSERT INTO ResearchBranches (btype, bname, abbr) VALUES (1, 'Unknown/Undecided', 'Un');
-- Fields within Physics
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 2, 'Nuclear and Particle Physics', 'Phys.NP' FROM ResearchBranches WHERE abbr = 'Phys';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 2, 'Condensed Matter Physics', 'Phys.CM' FROM ResearchBranches WHERE abbr = 'Phys';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 2, 'Astrophysics & Astronomy', 'Phys.AA' FROM ResearchBranches WHERE abbr = 'Phys';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 2, 'Atomic, Molecular, Optical & Quantum Physics', 'Phys.AMOQ' FROM ResearchBranches WHERE abbr = 'Phys';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 2, 'Miscellaneous Physics', 'Phys.MISC' FROM ResearchBranches WHERE abbr = 'Phys';
-- Areas within NP
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Experimental Nuclear Physics', 'Phys.NP.nucl-ex' FROM ResearchBranches WHERE abbr = 'Phys.NP';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Nuclear Theory', 'Phys.NP.nucl-th' FROM ResearchBranches WHERE abbr = 'Phys.NP';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Experimental Particle Physics', 'Phys.NP.hep-ex' FROM ResearchBranches WHERE abbr = 'Phys.NP';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Particle Theory', 'Phys.NP.hep-th' FROM ResearchBranches WHERE abbr = 'Phys.NP';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'NP Instrumentation & Methods', 'Phys.NP.im' FROM ResearchBranches WHERE abbr = 'Phys.NP';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Accelerator Physics', 'Phys.NP.accel' FROM ResearchBranches WHERE abbr = 'Phys.NP';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'NP Other Areas', 'Phys.NP.other' FROM ResearchBranches WHERE abbr = 'Phys.NP';
-- Areas within AA
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Cosmology', 'Phys.AA.cosmology' FROM ResearchBranches WHERE abbr = 'Phys.AA';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Galaxies', 'Phys.AA.galaxies' FROM ResearchBranches WHERE abbr = 'Phys.AA';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Stellar Physics & Astronomy', 'Phys.AA.stellar' FROM ResearchBranches WHERE abbr = 'Phys.AA';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Solar Physics', 'Phys.AA.solar' FROM ResearchBranches WHERE abbr = 'Phys.AA';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, '(Exo)Planets & Astrobiology)', 'Phys.AA.planetary' FROM ResearchBranches WHERE abbr = 'Phys.AA';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'High-Energy Astrophysical Processes
& Phenomena', 'Phys.AA.highenergy' FROM ResearchBranches WHERE abbr = 'Phys.AA';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'AA Instrumentation & Methods', 'Phys.AA.im' FROM ResearchBranches WHERE abbr = 'Phys.AA';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'AA Other Areas', 'Phys.AA.other' FROM ResearchBranches WHERE abbr = 'Phys.AA';
-- Areas within AMOQ
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Atomic Physics', 'Phys.AMOQ.atomic' FROM ResearchBranches WHERE abbr = 'Phys.AMOQ';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Optics', 'Phys.AMOQ.optics' FROM ResearchBranches WHERE abbr = 'Phys.AMOQ';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Molecular Physics', 'Phys.AMOQ.molecular' FROM ResearchBranches WHERE abbr = 'Phys.AMOQ';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Quantum Physics', 'Phys.AMOQ.quantum' FROM ResearchBranches WHERE abbr = 'Phys.AMOQ';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'AMOQ Other Areas', 'Phys.AMOQ.other' FROM ResearchBranches WHERE abbr = 'Phys.AMOQ';
-- Areas within CM
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Superconductivity & Superfluidity', 'Phys.CM.sc' FROM ResearchBranches WHERE abbr = 'Phys.CM';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Strongly Correlated Electron Systems', 'Phys.CM.sces' FROM ResearchBranches WHERE abbr = 'Phys.CM';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Soft Matter', 'Phys.CM.soft' FROM ResearchBranches WHERE abbr = 'Phys.CM';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Disordered Systems', 'Phys.CM.disordered' FROM ResearchBranches WHERE abbr = 'Phys.CM';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Mesoscale & Nanoscale Physics', 'Phys.CM.nano' FROM ResearchBranches WHERE abbr = 'Phys.CM';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Statistical Physics', 'Phys.CM.stat' FROM ResearchBranches WHERE abbr = 'Phys.CM';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'CM Other Areas', 'Phys.CM.other' FROM ResearchBranches WHERE abbr = 'Phys.CM';
-- Areas within MISC
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Biophysics', 'Phys.MISC.biophy' FROM ResearchBranches WHERE abbr = 'Phys.MISC';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Mathematical Physics', 'Phys.MISC.mathphy' FROM ResearchBranches WHERE abbr = 'Phys.MISC';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Chaos, Self-Organizing Systems & Nonlinear Physics', 'Phys.MISC.chaos' FROM ResearchBranches WHERE abbr = 'Phys.MISC';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Fluid Dynamics', 'Phys.MISC.fluid' FROM ResearchBranches WHERE abbr = 'Phys.MISC';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'MISC Other Areas', 'Phys.MISC.other' FROM ResearchBranches WHERE abbr = 'Phys.MISC';
-- Other Cross examples
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 2, 'History & Philosophy of Science', 'Cross.HPS' FROM ResearchBranches WHERE abbr = 'Cross';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'History of Science', 'Cross.HPS.hist' FROM ResearchBranches WHERE abbr = 'Cross.HPS';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Laws of Nature', 'Cross.HPS.laws' FROM ResearchBranches WHERE abbr = 'Cross.HPS';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Scientific Method', 'Cross.HPS.method' FROM ResearchBranches WHERE abbr = 'Cross.HPS';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Sociology of Science', 'Cross.HPS.sociology' FROM ResearchBranches WHERE abbr = 'Cross.HPS';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 2, 'Material Science', 'Cross.MS' FROM ResearchBranches WHERE abbr = 'Cross';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 2, 'Brain & Neuroscience', 'Cross.BRAIN' FROM ResearchBranches WHERE abbr = 'Cross';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Neurophysiology', 'Cross.BRAIN.physio' FROM ResearchBranches WHERE abbr = 'Cross.BRAIN';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Molecular & Cellular Neuroscience', 'Cross.BRAIN.mol-cell' FROM ResearchBranches WHERE abbr = 'Cross.BRAIN';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Cognitive Neuroscience', 'Cross.BRAIN.cogn' FROM ResearchBranches WHERE abbr = 'Cross.BRAIN';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Computational Neuroscience', 'Cross.BRAIN.comput' FROM ResearchBranches WHERE abbr = 'Cross.BRAIN';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Developmental Neuroscience', 'Cross.BRAIN.devel' FROM ResearchBranches WHERE abbr = 'Cross.BRAIN';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Systems Neuroscience', 'Cross.BRAIN.sys' FROM ResearchBranches WHERE abbr = 'Cross.BRAIN';
INSERT INTO ResearchBranches (parent_bID, btype, bname, abbr) SELECT bID, 3, 'Neurophilosophy', 'Cross.BRAIN.phil' FROM ResearchBranches WHERE abbr = 'Cross.BRAIN';

CREATE TABLE BranchParameters (  -- parameters that depend on different research branches 
    pname VARCHAR(255) NOT NULL,
    bID INT UNSIGNED NOT NULL,
    pvalue DOUBLE NOT NULL,
    valid DATETIME NOT NULL default '0000-00-00 00:00:00',  -- starting valid time
    ts DATETIME NOT NULL default CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,  -- update time
    is_current TINYINT(1) NOT NULL DEFAULT 1,  -- 1=the current parameter, 0=obsolete
    FOREIGN KEY (bID) REFERENCES ResearchBranches (bID),
    PRIMARY KEY (pname, bID, valid),
    INDEX (pname, bID, is_current)
);
-- branch-dependent double parameters (the latest modification (largest 'valid' time) will supercede the early values)
-- INSERT INTO BranchParameters (pname,bID,pvalue) VALUES ('f_n',0,1);   -- default=1, insert only if not 1.

CREATE TABLE TopicTypes (   -- in the format of Topic/FOCUS/question for emerging multi-field hot topics
    ID TINYINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    ttname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
--    is_active TINYINT(1) NOT NULL default 1,  -- 0 means a decommissioned type
    detail TEXT COLLATE utf8mb4_general_ci
);
INSERT INTO TopicTypes (ID,ttname,detail) VALUES (1,'Topic','top level; format: TitleCase or free');
INSERT INTO TopicTypes (ID,ttname,detail) VALUES (2,'Focus','2nd level; format: ALLCAPS or ACRONYM or free; special: MISC');
INSERT INTO TopicTypes (ID,ttname,detail) VALUES (3,'Question','3rd level; format: all-lowercases or free; special: other');
-- The topics may have chances to be incorporated into ResearchBranches as a new discipline later

CREATE TABLE ResearchTopics (  -- multi-parent-branch research subjects (emerging topics might become independently later); the hierarchy within the topic system itself is linear (single-parent)
    tID INT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    parent_tID INT UNSIGNED,        -- For hierarchical structure (NULL for top-level topic)
    ttype TINYINT UNSIGNED NOT NULL,     -- 1='Topic', 2='Focus', 3='Question'
    tname VARCHAR(255) COLLATE ascii_general_ci UNIQUE NOT NULL,  -- e.g., 'AI Technology'
    abbr VARCHAR(30) COLLATE ascii_general_ci UNIQUE NOT NULL,    -- e.g., 'AI', 'Xxx/XX/xx-xx'
--    f_n FLOAT NOT NULL default 1,     -- the normalization factor for ECP from both parent documents and derived contributions that accounts for differences in different branches
    is_active TINYINT(1) NOT NULL default 1,    -- 0 means a decommissioned research topic
    detail TEXT COLLATE utf8mb4_general_ci,     -- Optional detailed description
    -- ... metadata about the branch (e.g., key journals, representative topics)
    FOREIGN KEY (ttype) REFERENCES TopicTypes (ID),
    FOREIGN KEY (parent_tID) REFERENCES ResearchTopics (tID), -- Self-referencing for hierarchy
    INDEX (parent_tID, abbr, is_active),
    INDEX (ttype, abbr, is_active)                       -- Index to filter by branch type
);
INSERT INTO ResearchTopics (tID, ttype, tname, abbr) VALUES (1,1, 'All Topics', 'AllTopics');  -- a dummy representing all topic subjects; no sub-topics should be allowed
INSERT INTO ResearchTopics (ttype, tname, abbr) VALUES (1, 'Artificial Intelligence', 'AI');
INSERT INTO ResearchTopics (ttype, tname, abbr) VALUES (1, 'Quantum Computing', 'QC');
INSERT INTO ResearchTopics (parent_tID, ttype, tname, abbr) SELECT tID, 2, 'Machine Learning', CONCAT(abbr,'/MachL') FROM ResearchTopics WHERE abbr = 'AI';
INSERT INTO ResearchTopics (parent_tID, ttype, tname, abbr) SELECT tID, 2, 'Deep Learning', CONCAT(abbr,'/DeepL') FROM ResearchTopics WHERE abbr = 'AI';
INSERT INTO ResearchTopics (parent_tID, ttype, tname, abbr) SELECT tID, 2, 'Natural Language Processing', CONCAT(abbr,'/NLP') FROM ResearchTopics WHERE abbr = 'AI';
INSERT INTO ResearchTopics (parent_tID, ttype, tname, abbr) SELECT tID, 2, 'Computer Vision', CONCAT(abbr,'/CompV') FROM ResearchTopics WHERE abbr = 'AI';
INSERT INTO ResearchTopics (parent_tID, ttype, tname, abbr) SELECT tID, 2, 'Robotics', CONCAT(abbr,'/Rob') FROM ResearchTopics WHERE abbr = 'AI';
INSERT INTO ResearchTopics (parent_tID, ttype, tname, abbr) SELECT tID, 2, 'Generative AI', CONCAT(abbr,'/GenAI') FROM ResearchTopics WHERE abbr = 'AI';
INSERT INTO ResearchTopics (parent_tID, ttype, tname, abbr) SELECT tID, 2, 'Artificial General Intelligence', CONCAT(abbr,'/AGI') FROM ResearchTopics WHERE abbr = 'AI';


CREATE TABLE TopicBranches ( -- parent branches of a top-level topic
    tID INT UNSIGNED NOT NULL,
    bID INT UNSIGNED NOT NULL,
    FOREIGN KEY (tID) REFERENCES ResearchTopics (tID),
    FOREIGN KEY (bID) REFERENCES ResearchBranches (bID),
    PRIMARY KEY (tID,bID)
);
INSERT INTO TopicBranches (tID,bID) SELECT 2, bID FROM ResearchBranches WHERE abbr = 'Phys'; -- top level 'AI'
INSERT INTO TopicBranches (tID,bID) SELECT 2, bID FROM ResearchBranches WHERE abbr = 'Math'; -- top level 'AI'
INSERT INTO TopicBranches (tID,bID) SELECT 2, bID FROM ResearchBranches WHERE abbr = 'Comp'; -- top level 'AI'
INSERT INTO TopicBranches (tID,bID) SELECT 2, bID FROM ResearchBranches WHERE abbr = 'Engr'; -- top level 'AI'
INSERT INTO TopicBranches (tID,bID) SELECT 3, bID FROM ResearchBranches WHERE abbr = 'Phys'; -- top level 'QC'
INSERT INTO TopicBranches (tID,bID) SELECT 3, bID FROM ResearchBranches WHERE abbr = 'Math'; -- top level 'QC'
INSERT INTO TopicBranches (tID,bID) SELECT 3, bID FROM ResearchBranches WHERE abbr = 'Comp'; -- top level 'QC'
INSERT INTO TopicBranches (tID,bID) SELECT 3, bID FROM ResearchBranches WHERE abbr = 'Engr'; -- top level 'QC'


-- Member-related tables

CREATE TABLE RoleTypes (  -- IDs in between major roles can be used for subtypes of a role, e.g., Member0-Member9
    ID TINYINT UNSIGNED PRIMARY KEY,
    rtname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
    is_active TINYINT(1) NOT NULL default 1,    -- 0 means a decommissioned type
    detail TEXT COLLATE utf8mb4_general_ci,
    INDEX (rtname,is_active)
);
INSERT INTO RoleTypes (ID,rtname,detail) VALUES (1,'Guest','Visitor');
INSERT INTO RoleTypes (ID,rtname,detail) VALUES (10,'Member','entry-level membership');
INSERT INTO RoleTypes (ID,rtname,detail) VALUES (20,'Rater','eligible to review and rate on a topic');
INSERT INTO RoleTypes (ID,rtname,detail) VALUES (30,'Reviewer','eligible to review and rate in an area');
INSERT INTO RoleTypes (ID,rtname,detail) VALUES (40,'Moderator','eligible to moderate and chair committees in a field');
INSERT INTO RoleTypes (ID,rtname,detail) VALUES (50,'Leader','eligible to lead and serve as board directors in a discipline');
-- Rater and above must be identified by either affiliation or personal ID to prevent abuse of multiple IDs from a single person

CREATE TABLE AdminTypes (  -- IDs in between major roles can be used for subtypes, e.g., Editor0-Editor99
    ID SMALLINT UNSIGNED PRIMARY KEY,
    atname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
    is_active TINYINT(1) NOT NULL default 1,    -- 0 means a decommissioned type
    detail TEXT COLLATE utf8mb4_general_ci,
    INDEX (atname,is_active)
);
INSERT INTO AdminTypes (ID,atname,detail) VALUES (100,'Viewer','view only');
INSERT INTO AdminTypes (ID,atname,detail) VALUES (200,'Updater','update simple stuff');
INSERT INTO AdminTypes (ID,atname,detail) VALUES (300,'Editor','edit non-critical content');
INSERT INTO AdminTypes (ID,atname,detail) VALUES (400,'Senior Editor','edit more critical content');
INSERT INTO AdminTypes (ID,atname,detail) VALUES (500,'Chief Editor','edit most content');
INSERT INTO AdminTypes (ID,atname,detail) VALUES (600,'Administrator','full access/permission');

CREATE TABLE AuthorClasses (  -- fixed-scheme and variable-scheme, sum of all non-general authors <= 875%
    ID TINYINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    acname VARCHAR(3) COLLATE ascii_general_ci UNIQUE NOT NULL,
    duty TINYINT UNSIGNED,      -- significance in contribution (percentage) normalized to 100% of 1st-class
    duty_min TINYINT UNSIGNED,
    duty_max TINYINT UNSIGNED,
    is_fixed TINYINT(1) NOT NULL default 1,     -- 1 is fixed to use duty, otherwise use variable-scheme in between duty_min and duty_min
    is_active TINYINT(1) NOT NULL default 1,    -- 0 means a decommissioned type
    detail TEXT COLLATE utf8mb4_general_ci,
    INDEX (acname, is_active)
);
INSERT INTO AuthorClasses (ID,acname,duty) VALUES (1,'1st',100);   -- 1st-class  100%
INSERT INTO AuthorClasses (ID,acname,duty) VALUES (2,'2nd',50);    -- 2nd-class  50%
INSERT INTO AuthorClasses (ID,acname,duty) VALUES (3,'3rd',25);    -- 3rd-class  25%
INSERT INTO AuthorClasses (ID,acname,duty,duty_min,duty_max,is_fixed) VALUES (4,'var',75,20,99,0);   -- variable-class  20-99%
INSERT INTO AuthorClasses (ID,acname,duty) VALUES (5,'gen',10);    -- general class   10%

CREATE TABLE Countries ( -- list of countries for institutions below
    ID SMALLINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    cname VARCHAR(60) COLLATE utf8mb4_general_ci NOT NULL,
    is_active TINYINT(1) NOT NULL default 1,      -- 0=does not exist any more
    detail TEXT COLLATE utf8mb4_general_ci,        -- full name and/or period of existence
    INDEX (cname,is_active)
);
INSERT INTO Countries (ID,cname,detail) VALUES (0,'Independent','affiliated with no country/region');
INSERT INTO Countries (ID,cname,detail) VALUES (1,'USA','United States of America');

CREATE TABLE InstitutionTypes ( -- types of top level institutions a member is primarily affiliated with
    ID TINYINT UNSIGNED PRIMARY KEY,
    itname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
    is_active TINYINT(1) NOT NULL default 1,      -- 0=decommissioned type
    detail TEXT COLLATE utf8mb4_general_ci,
    INDEX (itname,is_active)
);
INSERT INTO InstitutionTypes (ID,itname,detail) VALUES (0,'Below_Colleges','the other creditable institutions like high schools');
INSERT INTO InstitutionTypes (ID,itname,detail) VALUES (1,'Teaching_Colleges','top 100,000 typically for bachelor degrees');
INSERT INTO InstitutionTypes (ID,itname,detail) VALUES (2,'Master_Level','top 20,000');
INSERT INTO InstitutionTypes (ID,itname,detail) VALUES (3,'PhD_Level','top 10,000');
INSERT INTO InstitutionTypes (ID,itname,detail) VALUES (4,'Major_Research','top 1,000');
INSERT INTO InstitutionTypes (ID,itname,detail) VALUES (5,'Top_Research','top 100');

CREATE TABLE Institutions ( -- top level institutions (no sub-units like departments should be included)
    iID INT UNSIGNED PRIMARY KEY AUTO_INCREMENT,  -- unique identifier for each institution
    iname VARCHAR(255) COLLATE utf8mb4_general_ci UNIQUE NOT NULL,   -- "University of Notre Dame (USA)"
    itype TINYINT UNSIGNED NOT NULL default 0,    -- 0=below colleges, 1=bachelor colleges(100k), 2=master-level(20k), 3=PhD-level(10k), 4=Major_Research(1k), 5=Top_Research(100)
    is_active TINYINT(1) NOT NULL default 1,      -- 0=does not exist any more
    icountry SMALLINT UNSIGNED NOT NULL default 0,     -- 0=not belong to any country
    FOREIGN KEY (icountry) REFERENCES Countries (ID),
    FOREIGN KEY (itype) REFERENCES InstitutionTypes (ID),
    INDEX (iname,icountry,itype,is_active),
    FULLTEXT INDEX (iname)
);
INSERT INTO Institutions (iID,iname) VALUES (1,'Independent');
INSERT INTO Institutions (iID,iname,itype,icountry) VALUES (2,'University of Notre Dame (USA)',5,1);
-- no leading 'the', no sub-units, should end with (country/region) for distinction.

CREATE TABLE Members (  -- IT personnel of CORE with CORE-ID <= ZZZ (3 digits); regular members (at least 4 digits)
    mID BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,          -- Unique identifier for each member
    ID_alphanum VARCHAR(9) COLLATE ascii_general_ci UNIQUE, -- (CONV(mID,10,36)), alphanumerical format of member ID; CORE-ID display format xxx-xxx-xxx
    ORCID CHAR(19) COLLATE ascii_general_ci UNIQUE,     -- ORCID ID as unique identifier
    family_name VARCHAR(255) COLLATE utf8mb4_general_ci NOT NULL,
    first_name VARCHAR(255) COLLATE utf8mb4_general_ci NOT NULL default '',    -- empty string '' if a member has no first name
    display_name VARCHAR(255) COLLATE utf8mb4_general_ci NOT NULL default (IF(first_name = '', family_name, CONCAT(first_name, ' ', family_name))),    -- for display on the member page
    pub_name VARCHAR(255) COLLATE utf8mb4_general_ci NOT NULL default (IF(first_name = '', family_name, CONCAT(SUBSTRING(first_name,1,1),  '. ', family_name))),   -- for use in publications
    iID INT UNSIGNED NOT NULL default 1,  -- primary institution that the member is affiliated with; default='Independent'
    email VARCHAR(254) COLLATE utf8mb4_general_ci UNIQUE,  -- unique email address for each member
    pass VARCHAR(255),   -- password hash; ORCID authentication only if NULL
    token VARCHAR(64),  -- for persistent logins; NULL represents an inactive user
    email_verified TINYINT(1) DEFAULT 0,   -- email verification status
    is_email_public TINYINT(1) NOT NULL default 0,  -- show email to the public or not
    is_good TINYINT(1) NOT NULL default 1,          -- true for good standing
    is_active TINYINT(1) NOT NULL default 1,	    -- true for active member; inactive after a year without login or other reasons
    is_deceased TINYINT(1) NOT NULL default 0,
    avatar VARCHAR(30),     -- 'path/to/avatar.svg' in storage/avatars/; 1=default/core, 2=admin, 3=anonym, 4=semianonym
    mail_areas VARCHAR(255),       -- semicolon-separated IDs of branches that the member subscribes to for receiving daily mailings; private only
    work_areas VARCHAR(255),       -- semicolon-separated IDs of branches that the member is working on; negative means not to show to the public
    interest_areas VARCHAR(255),   -- semicolon-separated IDs of branches that the member is interested in and subscribes to; negative means not to show to the public
    mrole TINYINT UNSIGNED NOT NULL default 10,   -- highest role: 10=member, 20=rater, 30=reviewer, 40=moderator, 50=leader
    admin_role SMALLINT UNSIGNED NOT NULL default 100,  -- 100=viewer, 200=updater, 300=editor, 400=senior editor, 500=chief editor, 600=administrator
    timezone VARCHAR(32) COLLATE utf8mb4_general_ci NOT NULL default 'UTC',  -- "Timezone/Location" format, default 'UTC'
--  lang VARCHAR(32) COLLATE ascii_general_ci default 'en',
    start_membership DATETIME DEFAULT CURRENT_TIMESTAMP,    -- start time as a member in CORE
    last_login DATETIME DEFAULT CURRENT_TIMESTAMP,    -- last login time
    AL VARCHAR(6) COLLATE ascii_general_ci NOT NULL default '0',   -- Achievement Level, e.g., "L2.5/13", "L-.5/10", "L-1/99" (where 99 is the number difference between A- and A+ docs) without the leading 'L', -- Ln/m <=> alpha > m(6^(n+0.5)-1)
    ALS DOUBLE NOT NULL default 0,     -- achievement level score = alpha = top five + others (A>2.5)
    ECP DOUBLE NOT NULL DEFAULT 10e0,  -- Total Earned Credit Points (initially 10 points)
    FOREIGN KEY (iID) REFERENCES Institutions (iID),
    FOREIGN KEY (mrole) REFERENCES RoleTypes (ID),
    FOREIGN KEY (admin_role) REFERENCES AdminTypes (ID),
    INDEX (pub_name, iID, mrole, is_active),
    INDEX (family_name, first_name, is_active)
);
ALTER TABLE Members AUTO_INCREMENT = 46656;  -- regular members starts with four digits of ID_alphanum (CORE-ID); shorter ones are reserved.
-- after each INSERT, UPDATE Members SET ID_alphanum = CONV(mID,10,36) WHERE ID_alphanum IS NULL;
-- can log in via ORCID, or ID_alphanum (stripped leading 0s and hyphens) / email + password
-- to use a nice-looking member-ID in php and web display: wordwrap(str_pad(ID_alphanum,9,"0",STR_PAD_LEFT),3,"-",true), in php ID_alphanum = base_convert(mID,10,36)

CREATE TABLE EmailTokens (  -- sending member an email with token to either verify the email address or to reset password
    mID BIGINT UNSIGNED NOT NULL,
    token_hash VARCHAR(64) NOT NULL UNIQUE,  -- make sure no collision is allowed
    token_type ENUM('verify_email', 'reset_password') NOT NULL,
    expires_at DATETIME NOT NULL,  -- verify_email expires in 48 hrs while reset_password in 30 minutes
    PRIMARY KEY (mID, token_type), -- Ensures only one active token per type
    FOREIGN KEY (mID) REFERENCES Members(mID) ON DELETE CASCADE
);

CREATE TABLE MemberMetaKeys (  -- optional member information
    ID TINYINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    mkname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
    is_active TINYINT(1) NOT NULL default 1,    -- 0 means a decommissioned key
    detail TEXT COLLATE utf8mb4_general_ci,
    INDEX (mkname, is_active)
);
INSERT INTO MemberMetaKeys (ID,mkname,detail) VALUES (1,'full_name','Full Name');
INSERT INTO MemberMetaKeys (ID,mkname,detail) VALUES (2,'other_names','semicolon-separated other names');
INSERT INTO MemberMetaKeys (ID,mkname,detail) VALUES (3,'prefix','Name Prefix');
INSERT INTO MemberMetaKeys (ID,mkname,detail) VALUES (4,'suffix','Name Suffix');
INSERT INTO MemberMetaKeys (ID,mkname,detail) VALUES (5,'position','job title');
INSERT INTO MemberMetaKeys (ID,mkname,detail) VALUES (6,'affiliations','semicolon-separated affiliations');
INSERT INTO MemberMetaKeys (ID,mkname,detail) VALUES (7,'address','mailing address');
INSERT INTO MemberMetaKeys (ID,mkname,detail) VALUES (8,'url1','Professional HomePage');
INSERT INTO MemberMetaKeys (ID,mkname,detail) VALUES (9,'url2','Personal HomePage');
INSERT INTO MemberMetaKeys (ID,mkname,detail) VALUES (10,'education','semicolon-separated (year, degree, major, institution=iID)');
INSERT INTO MemberMetaKeys (ID,mkname,detail) VALUES (11,'cv','Link to CV');
INSERT INTO MemberMetaKeys (ID,mkname,detail) VALUES (12,'research_statement','Research Statement');
INSERT INTO MemberMetaKeys (ID,mkname,detail) VALUES (13,'other_interests','hobbies, leisure activities');
INSERT INTO MemberMetaKeys (ID,mkname,detail) VALUES (14,'mstatus','self-claimed member status information (retired, on vacation, no access, etc.)');

CREATE TABLE MemberMeta (  -- optional member information to be filled
    ID BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    mID BIGINT UNSIGNED NOT NULL,
    meta_ID TINYINT UNSIGNED NOT NULL,
    meta_value TEXT COLLATE utf8mb4_general_ci NOT NULL,
    is_public TINYINT(1) NOT NULL default 0,    -- if member wants to show it to the public or not
    FOREIGN KEY (mID) REFERENCES Members(mID),
    FOREIGN KEY (meta_ID) REFERENCES MemberMetaKeys(ID),
    UNIQUE (mID, meta_ID),
    INDEX (meta_ID,is_public)
);
-- e.g., INSERT INTO MemberMeta (mID,meta_ID,meta_value) VALUES (1,2,'a test') ON DUPLICATE KEY UPDATE  meta_value = VALUES(meta_value);

CREATE TABLE MembershipHistory (  -- record starting time of membership roles (mrole); the current mrole is with the latest 'ts'
    mID BIGINT UNSIGNED NOT NULL,
    mrole TINYINT UNSIGNED NOT NULL,
    ts DATETIME DEFAULT CURRENT_TIMESTAMP,    -- starting time of this role
    FOREIGN KEY (mID) REFERENCES Members(mID),
    FOREIGN KEY (mrole) REFERENCES RoleTypes (ID),
    PRIMARY KEY (mID, mrole)
);
-- e.g., INSERT INTO MembershipHistory (mID,mrole) VALUES (1,20);

CREATE TABLE ALSHistory ( -- weekly update if necessary
    ID BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    mID BIGINT UNSIGNED NOT NULL,
    ALS DOUBLE NOT NULL,
    ts DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (mID) REFERENCES Members(mID),
    INDEX (mID)
);

CREATE TABLE ECPHistory ( -- weekly update if necessary
    ID BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    mID BIGINT UNSIGNED NOT NULL,
    ECP DOUBLE NOT NULL,
    ts DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (mID) REFERENCES Members(mID),
    INDEX (mID)
);

CREATE TABLE MemberRoles (     --  member list with their current role of ratership or above for a given research branch
    mID BIGINT UNSIGNED NOT NULL,
    bID INT UNSIGNED NOT NULL,
    mrole TINYINT UNSIGNED NOT NULL,
    ts DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (mID) REFERENCES Members(mID),
    FOREIGN KEY (bID) REFERENCES ResearchBranches(bID),
    FOREIGN KEY (mrole) REFERENCES RoleTypes (ID),
    PRIMARY KEY (mID, bID),
    INDEX (bID)
);


-- Committee-related tables

CREATE TABLE CommTypes (   -- committee types
    ID TINYINT UNSIGNED PRIMARY KEY,
    ctname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
    is_active TINYINT(1) NOT NULL default 1,  -- 0 means a decommissioned type
    detail TEXT COLLATE utf8mb4_general_ci,
    INDEX (ctname,is_active)
);
INSERT INTO CommTypes (ID,ctname,detail) VALUES (1,'CORE Executive','entire community-wide, disciplinary-wide, field-wide');
INSERT INTO CommTypes (ID,ctname,detail) VALUES (2,'Task Force','long-term task force committees like NSAC');
INSERT INTO CommTypes (ID,ctname,detail) VALUES (3,'PAC/Review Panel','PAC at user-facilities, Panel for advisory activities');
INSERT INTO CommTypes (ID,ctname,detail) VALUES (4,'Investigation','Investigate critical issues in the community');
INSERT INTO CommTypes (ID,ctname,detail) VALUES (5,'Research Moderation','Make moderation decisions on frauds and new phases at CORE');
INSERT INTO CommTypes (ID,ctname,detail) VALUES (6,'CORE Issues','Modify AL/EPS formulas / Decide on bugs/new features/issues at CORE');
INSERT INTO CommTypes (ID,ctname,detail) VALUES (7,'Conference/Meeting','Organizing conferences, meetings, workshops, summer schools');
INSERT INTO CommTypes (ID,ctname,detail) VALUES (8,'Prize/Award', 'A committee for a prize or award');
INSERT INTO CommTypes (ID,ctname,detail) VALUES (9,'Academic Hiring', 'for hiring in academia');
INSERT INTO CommTypes (ID,ctname,detail) VALUES (255,'Ad hoc','temporary committee for a particular issue or task');

CREATE TABLE Committees (       -- CORE committees
    ID INT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    commtype TINYINT UNSIGNED NOT NULL, 
    parent_ID INT UNSIGNED,           -- not null if it is a sub-committee
    commname VARCHAR(255) COLLATE utf8mb4_general_ci NOT NULL,
    creator BIGINT UNSIGNED NOT NULL,       -- committee creator
    is_active TINYINT(1) NOT NULL default 1,
    email VARCHAR(254) COLLATE utf8mb4_general_ci,
    contact VARCHAR(255) COLLATE utf8mb4_general_ci,
    task TEXT COLLATE utf8mb4_general_ci,
    UNIQUE (commname, commtype),
    FOREIGN KEY (commtype) REFERENCES CommTypes (ID),
    FOREIGN KEY (creator) REFERENCES Members(mID),
    FOREIGN KEY (parent_ID) REFERENCES Committees(ID), -- Self-referencing for hierarchy
    INDEX (parent_ID, commtype, is_active)
);

CREATE TABLE CommitteeBranches (  -- a committee can be related to several branches or no branch at all
    comm_ID INT UNSIGNED NOT NULL,
    bID INT UNSIGNED NOT NULL,
    impact TINYINT UNSIGNED NOT NULL default 100,    -- impact percentage points on this branch, also used for ECP, no more than 100% for the sum
    FOREIGN KEY (comm_ID) REFERENCES Committees(ID),
    FOREIGN KEY (bID) REFERENCES ResearchBranches(bID),
    PRIMARY KEY (comm_ID, bID)
);
-- e.g., INSERT INTO CommitteeBranches (comm_ID, bID, impact) VALUES (1,2,33) ON DUPLICATE KEY UPDATE impact = VALUES(impact);

CREATE TABLE CommitteeMembers (    -- for non-general classes only, duty=20-100; gen-class is not allowed; sum of all committee members: duty <=875
    comm_ID INT UNSIGNED NOT NULL,
    mID BIGINT UNSIGNED NOT NULL,
    term TINYINT UNSIGNED NOT NULL default 1,         -- the n-th term
    duty TINYINT UNSIGNED NOT NULL default 100,       -- Significance/responsibility/duty percentage in contribution normalized to 100% of 1st-class
    position VARCHAR(60) COLLATE utf8mb4_general_ci,  -- 'chair', 'vice chair', 'member', 'non-voting member'
    start_date DATE DEFAULT CURRENT_DATE,
    end_date DATE,
    FOREIGN KEY (comm_ID) REFERENCES Committees(ID),
    FOREIGN KEY (mID) REFERENCES Members(mID),
    PRIMARY KEY (comm_ID, mID, term)  -- can serve multiple terms
);


-- DocType-related tables (including associated parameters)

CREATE TABLE VisibilityTypes (  -- for both documents and comments/reviews
    ID TINYINT UNSIGNED PRIMARY KEY,
    vtname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
    is_active TINYINT(1) NOT NULL default 1,  -- 0 means a decommissioned type
    detail TEXT COLLATE utf8mb4_general_ci,
    INDEX (vtname,is_active)
);
INSERT INTO VisibilityTypes (ID,vtname,detail) VALUES (1,'Visible to all','Published and viewable by everybody');
INSERT INTO VisibilityTypes (ID,vtname,detail) VALUES (10,'Visible to members','not shown to the general public');
INSERT INTO VisibilityTypes (ID,vtname,detail) VALUES (20,'Visible to raters+','Raters and above can see');
INSERT INTO VisibilityTypes (ID,vtname,detail) VALUES (30,'Visible to reviewers+','Reviewers and above');
INSERT INTO VisibilityTypes (ID,vtname,detail) VALUES (40,'Visible to moderators+','Moderators and above');
INSERT INTO VisibilityTypes (ID,vtname,detail) VALUES (50,'Visible to leaders+','Leaders and above');
INSERT INTO VisibilityTypes (ID,vtname,detail) VALUES (60,'Ready to be announced','will be announced automatically after 24 hrs');
INSERT INTO VisibilityTypes (ID,vtname,detail) VALUES (80,'Under moderation','To be voted by raters+');
INSERT INTO VisibilityTypes (ID,vtname,detail) VALUES (89,'Rejected by moderation','Negative voting decision is made');
INSERT INTO VisibilityTypes (ID,vtname,detail) VALUES (99,'Visible to submitter/authors only','for draft not ready for publication');
INSERT INTO VisibilityTypes (ID,vtname,detail) VALUES (100,'Visible to administrators only','System protected');
-- <60 for announced items with DOIs; 90-99 for private purposes; 80-89 for items under moderation and unannounced that cannot be rated/commented, if approved, it will be set to 60; Announcement will be done through an auto-script after 24 hrs; revision history will be kept only after announcement.
-- mrole >= visibility to view the announced document

CREATE TABLE DocGoals (
    ID TINYINT UNSIGNED PRIMARY KEY,
    dgname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
    is_active TINYINT(1) NOT NULL default 1,  -- 0 means a decommissioned type
    detail TEXT COLLATE utf8mb4_general_ci,
    INDEX (dgname,is_active)
);
INSERT INTO DocGoals (ID,dgname,detail) VALUES (1,'AL','for achievement level');
INSERT INTO DocGoals (ID,dgname,detail) VALUES (2,'ECP','to earn credit');
INSERT INTO DocGoals (ID,dgname,detail) VALUES (3,'Funding','to obtain funding support');
INSERT INTO DocGoals (ID,dgname,detail) VALUES (4,'Facility Time','to get time for user facility');
INSERT INTO DocGoals (ID,dgname,detail) VALUES (254,'Other Resources','to get other resources or benefits');
INSERT INTO DocGoals (ID,dgname,detail) VALUES (255,'No goals','to look for no benefits');

CREATE TABLE DocTypes (
    ID TINYINT UNSIGNED PRIMARY KEY,
    dtname VARCHAR(255) COLLATE ascii_general_ci UNIQUE NOT NULL,    -- "Original Research"
    abbr VARCHAR(2) COLLATE ascii_general_ci UNIQUE NOT NULL,     -- e.g., "o" for "o-doc"
    goal TINYINT UNSIGNED NOT NULL,     -- 1 for AL(o/a-doc), 2 for ECP, 3 for funding, 4 for facility time
    f_p DOUBLE NOT NULL default 0.1,       -- the factor for ECP calculation of derived contributions that accounts for different types of parent documents
    f_type DOUBLE NOT NULL default 0.1,    -- the factor for ECP calculation of parent documents due to their different types
    is_active TINYINT(1) NOT NULL default 1,  -- 0 means a decommissioned type
    detail TEXT COLLATE utf8mb4_general_ci,   -- optional detailed description
    FOREIGN KEY (goal) REFERENCES DocGoals (ID),
    INDEX (goal, abbr, is_active)
);
-- if goal>=3, then f_type=0
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (1,'Original Research','o',1,1,0.1,'main document type for innovation');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (2,'Review, Expository, and Survey Articles','r',2,0.5,1,'');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (3,'Synthesized Achievements for Evaluation','a',1,0.25,0,'collection of o/r-docs');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (4,'Textbooks, Lectures, and Other Books','b',2,0.25,1,'non-original research');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (5,'Conference/Meeting/School Organization','c',2,0.1,0.25,'conferences, meetings, workshops, summer schools');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (6,'Discussions on Community Issues','d',2,0.5,0.1,'forums in CORE community');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (7,'Pop-Science Essays','e',2,0.1,0.25,'for general audiences');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (8,'Funding Requests for Facility-Oriented Programs','f',3,0.5,0,'Large projects for building new facilities, establishing new centers or collaboration networks');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (9,'Funding Requests for Project-Oriented Research','p',3,1,0,'emphasis on high-risk high-reward projects');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (10,'Funding Requests for Equipment R&D','q',3,0.5,0,'building high-precision/cutting-edge experimental equipment and reaching the limits of measurements');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (11,'Funding Requestts for Service/Support Activities','s',3,0.5,0,'meetings, etc.');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (12,'Time Allocation Requests at Research Facilities','t',4,0.5,0,'access to user facilities');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (13,'Open Source Scientific Softwares','u',2,0.5,1,'codes for end users; highly rated ones could be funded');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (14,'Open Experimental Equipment','x',2,0.5,1,'encourage sharing of experimental equipment');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (15,'Voting for Election and Awards','v',2,0.1,0.1,'including committee work, nomination, comments, and actual votes');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (16,'White Papers and Other Official Reports','w',2,0.25,0.5,'important for mainstream research from CORE and other community committees');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (17,'Issues and/or Bugs in the CORE System','i',2,0.5,0.1,'for the website and databases of CORE');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (18,'New Suggestions for the CORE System','n',2,0.5,0.1,'request for new features, modifications to improve CORE');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (19,'Moderation and Investigation Reports','m',2,0.25,0.25,'acknowledge contributions from moderators');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (252,'Applications for Other Funding Opportunities','zw',3,0.1,0,'seeking other funding possibilities');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (253,'Other Service Documents','zx',2,0.1,0.1,'to earn ECP in other ways');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (254,'Miscellaneous Documents','zy',254,0.1,0,'seeking other benefits and resources');
INSERT INTO DocTypes (ID,dtname,abbr,goal,f_p,f_type,detail) VALUES (255,'No Documents','zz',255,0,0,'placeholder for no documents');

CREATE TABLE DocTypeParameters (  -- parameters that depend on different doc types 
    pname VARCHAR(255) NOT NULL,
    dtype TINYINT UNSIGNED NOT NULL,  -- doc_type
    pvalue DOUBLE NOT NULL,
    valid DATETIME NOT NULL,  -- starting valid time
    ts DATETIME NOT NULL default CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,  -- update time
    is_current TINYINT(1) NOT NULL DEFAULT 1,  -- 1=the current parameter, 0=obsolete
    FOREIGN KEY (dtype) REFERENCES DocTypes(ID),
    PRIMARY KEY (pname, dtype, valid),
    INDEX (pname, dtype, is_current)
);
-- only insert newly modified parameters (initial ones in the table DocTypes)
-- INSERT INTO DocTypeParameters (pname,dtype,pvalue,valid) VALUES ('f_p',1,1,NOW());
-- INSERT INTO DocTypeParameters (pname,dtype,pvalue,valid) VALUES ('f_type',1,0.1,NOW());


-- Document-related tables

CREATE TABLE Documents (
    dID BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,    -- unique identifier of a parent document
    ID_mini INT UNSIGNED,   -- initially NULL, then calculated and assigned right before announcement
    doi VARCHAR(15) COLLATE ascii_general_ci UNIQUE, -- initially NULL, then ={pubdate}.CONV(ID_mini,10,36) at announcement
--    ID_alphanum VARCHAR(12) COLLATE ascii_general_ci UNIQUE, -- NOT NULL default (CONV(parent_doc_ID,10,36)), -- alphanumerical format of parent_doc_ID
    dtype TINYINT UNSIGNED NOT NULL default 1,          -- e.g., 'o-docs' (default), 'r-docs', 'p-docs'
    title TEXT COLLATE utf8mb4_general_ci NOT NULL,
    abstract TEXT COLLATE utf8mb4_general_ci NOT NULL,
    notes VARCHAR(255) COLLATE utf8mb4_general_ci,     -- page#, fig#, other brief comments or changes for new revision
    submitter_ID BIGINT UNSIGNED NOT NULL,             -- member ID of the submitter
    author_list JSON,        -- JSON array of authors without affiliations: [[author_name=pub_name, mID , significance_factor=duty], ...] (author list in order, editors for c/m/w docs, organizers for f/s/t docs), null means submitter is the sole author  -- more complex: {"authors" : [[pub_name, mID, duty, [list of order numbers of affiliations]], ...], "affiliations": [[order#, name], ...]}
    visibility TINYINT UNSIGNED NOT NULL default 60,   -- 99=visible to submitter/authors only (for draft) , 1=visible to all, 10=visible to members, 20=visible to raters+, 30=visible to reviewers+, 40=visible to moderators+, 50=visible to leaders+, 100=visible to administrators only; default to 60=ready to be announced
    version TINYINT UNSIGNED NOT NULL default 1,   -- = 0 (no main file); 1+ = current version# of the main document starting with 1, maybe max of 16
    ver_suppl TINYINT UNSIGNED,                    -- NULL if no suppl file; 1+ =current version# of the supplementary file starting with 1 if uploaded, maybe max of 16
    main_size INT UNSIGNED,  -- size in bytes for the main pdf file (pdf only)
    suppl_size INT UNSIGNED,  -- size in bytes for the suppl file (pdf or zip)
    suppl_ext VARCHAR(7) COLLATE ascii_general_ci,  -- suppl file type: NULL if no suppl file, currently (pdf, zip) supported (possible future extensions: gz,7z,rar,tgz,tar.gz,tar.bz2)
    main_pages SMALLINT UNSIGNED,  -- number of pages in main file
    main_figs SMALLINT UNSIGNED,  -- number of figures in main file
    main_tabs SMALLINT UNSIGNED,  -- number of tables in main file
    datetime_added DATETIME DEFAULT CURRENT_TIMESTAMP,   -- datetime added to database
    submission_time DATETIME DEFAULT (datetime_added),   -- receive date (journal), posted date/time (preprint) for old doc / current datetime UTC at submission for new doc / deadline for v-doc
    pubdate VARCHAR(10) COLLATE ascii_general_ci,         -- YYYY-MM-DD, YYYY-MM, or YYYY derived from submission_time unless for journal it is publication date; should never change once assigned and announced so that it is consistent with doi
    announce_time DATETIME,               -- initially NULL, set the time when announced (passed screening and assigned with doi etc.)
    last_revision_time DATETIME DEFAULT (IF(version = 0, NULL, datetime_added)), -- NULL if no pdf attached
    last_update_time DATETIME DEFAULT (datetime_added) ON UPDATE CURRENT_TIMESTAMP,
    revision_history JSON,                         -- (JSON_ARRAY(JSON_ARRAY(version, ver_suppl, suppl_ext, last_revision_time, revison_notes, main_size, suppl_size)))
    Tresp SMALLINT UNSIGNED NOT NULL default 14,   -- responding time for the f_t factor in unit of days (typically 2 weeks for 20 pages or less, additional 1 week per extra 10 pages)
    nphase TINYINT UNSIGNED NOT NULL DEFAULT 1,    -- current phase#
    reset_time DATETIME NOT NULL DEFAULT (submission_time), -- starting time of this phase
    Ntot_invited TINYINT UNSIGNED NOT NULL default 0,       -- total# of invited reviews during this phase, not all docs required
    Ntot_contributed TINYINT UNSIGNED NOT NULL default 0,   -- total# of contributed reviews during this phase, not all required
    Ntot_comments INT UNSIGNED NOT NULL default 0, -- total# of creditable comments during this phase
    Ntot_ratings INT UNSIGNED NOT NULL default 0,  -- total# of ratings during this phase
    S_ave DOUBLE NOT NULL default 0,     -- average rating (Aj for o-doc; Asyn for a-doc, <S> for others)
    ECP DOUBLE NOT NULL default 0,       -- ECP of this doc
    full_text LONGTEXT COLLATE utf8mb4_general_ci, -- Storing text content for search or retrieval (optional)
    -- ... more metadata
    FOREIGN KEY (dtype) REFERENCES DocTypes(ID),
    FOREIGN KEY (submitter_ID) REFERENCES Members(mID),
    FOREIGN KEY (visibility) REFERENCES VisibilityTypes(ID),
    UNIQUE(pubdate,ID_mini),
    INDEX (doi),
    INDEX (dtype, announce_time, visibility),      -- Index for document type in the order of announce_time
    INDEX (dtype, S_ave),
    INDEX (submitter_ID),
    FULLTEXT INDEX (title, abstract)     -- Fulltext index for title/abstract search
);
-- update revision_history before insert a new revision: UPDATE Documents SET revision_history = JSON_ARRAY_APPEND(revision_history, '$', JSON_ARRAY(version, last_revision_time, notes));
-- overall_ columns saved to a separate table (nphase=0), previous phases need to be recalculated according to reset_time

CREATE TABLE ExternalTypes (
    ID TINYINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    etname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
    is_active TINYINT(1) NOT NULL default 1,  -- 0 means a decommissioned type
    detail TEXT COLLATE utf8mb4_general_ci,
    INDEX (etname,is_active)
);
INSERT INTO ExternalTypes (ID,etname,detail) VALUES (1,'Unknown','unknown type of sources');
INSERT INTO ExternalTypes (ID,etname,detail) VALUES (2,'Journal','peer-reviewed journal publications');
INSERT INTO ExternalTypes (ID,etname,detail) VALUES (3,'Preprint','various e-preprint services');
INSERT INTO ExternalTypes (ID,etname,detail) VALUES (4,'Review/Comment','external review or comment links');
INSERT INTO ExternalTypes (ID,etname,detail) VALUES (5,'MultiMedia','multimedia links');

-- build a large source list table including all canonical/abbr names of journals, preprint services, and other sources
CREATE TABLE ExternalSources (
    sID INT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    ext_type TINYINT UNSIGNED NOT NULL default 2,  -- default is Journal
    esname VARCHAR(30) COLLATE ascii_general_ci UNIQUE NOT NULL, -- source name like PRL, arXiv, etc; could be inferred directly from the doi/url link
    is_doi TINYINT(1) NOT NULL default 1,     -- 0 means DOI is not required; 1 = must be DOI link
    is_active TINYINT(1) NOT NULL default 1,  -- 0 means a decommissioned source
    pattern VARCHAR(60) COLLATE ascii_general_ci,  -- matching pattern in doi / URL for this source; unfortunately it may change over time and may not be unique
    publisher VARCHAR(30) COLLATE ascii_general_ci, -- used only for journals; null =  unknown publisher or not needed as esname is self-evident
    detail TEXT COLLATE utf8mb4_general_ci,
    FOREIGN KEY (ext_type) REFERENCES ExternalTypes(ID),
    INDEX (ext_type,esname,is_active)
);
INSERT INTO ExternalSources (sID,ext_type,esname,detail) VALUES (1,1,'URL','Not yet identified URL web link');
INSERT INTO ExternalSources (sID,ext_type,esname,detail) VALUES (2,1,'DOI','Not yet identified DOI link');
INSERT INTO ExternalSources (sID,ext_type,esname,pattern,publisher,detail) VALUES (3,2,'PRL','10.1103/PhysRevLett.','APS','Physical Review Letters');
INSERT INTO ExternalSources (sID,ext_type,esname,pattern,detail) VALUES (4,3,'arXiv','10.48550/arXiv.','arXiv.org');
INSERT INTO ExternalSources (sID,ext_type,esname,pattern,publisher,detail) VALUES (5,2,'PRX','10.1103/PhysRevX.','APS','Physical Review X');
INSERT INTO ExternalSources (sID,ext_type,esname,pattern,publisher,detail) VALUES (6,2,'Phys.Rev.','10.1103/PhysRev.','APS','Physical Review');
INSERT INTO ExternalSources (sID,ext_type,esname,pattern,publisher,detail) VALUES (7,2,'PRA','10.1103/PhysRevA.','APS','Physical Review A');
INSERT INTO ExternalSources (sID,ext_type,esname,pattern,publisher,detail) VALUES (8,2,'PRB','10.1103/PhysRevB.','APS','Physical Review B');
INSERT INTO ExternalSources (sID,ext_type,esname,pattern,publisher,detail) VALUES (9,2,'PRC','10.1103/PhysRevC.','APS','Physical Review C');
INSERT INTO ExternalSources (sID,ext_type,esname,pattern,publisher,detail) VALUES (10,2,'PRD','10.1103/PhysRevD.','APS','Physical Review D');
INSERT INTO ExternalSources (sID,ext_type,esname,pattern,publisher,detail) VALUES (11,2,'PRE','10.1103/PhysRevE.','APS','Physical Review E');
INSERT INTO ExternalSources (sID,ext_type,esname,pattern,publisher,detail) VALUES (12,2,'PLA','10.1016/j.physleta.','Elsevier','Physics Letters A');
INSERT INTO ExternalSources (sID,ext_type,esname,pattern,publisher,detail) VALUES (13,2,'PLB','10.1016/j.physletb.','Elsevier','Physics Letters B');
INSERT INTO ExternalSources (sID,ext_type,esname,pattern,publisher,detail) VALUES (14,2,'Int.J.Mod.Phys.D','10.1142/S02182718','World Scientific','International Journal of Modern Physics D');
INSERT INTO ExternalSources (sID,ext_type,esname,pattern,publisher,detail) VALUES (15,2,'Universe','10.3390/universe','MDPI','Universe');
INSERT INTO ExternalSources (sID,ext_type,esname,pattern,publisher,detail) VALUES (16,2,'Symmetry','10.3390/sym','MDPI','Symmetry');
INSERT INTO ExternalSources (sID,ext_type,esname,pattern,detail) VALUES (17,3,'OSF','10.31219/osf.io','OSF Preprints');
INSERT INTO ExternalSources (sID,ext_type,esname,pattern,detail) VALUES (18,3,'Preprints.org','10.20944/preprints','Preprints.org by MDPI');

CREATE TABLE ExternalDocs ( -- external DOIs/links/sources to documents (typically o/r-docs)
    ID BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    dID BIGINT UNSIGNED NOT NULL,
    sID INT UNSIGNED NOT NULL,
    esname VARCHAR(30) COLLATE ascii_general_ci, -- user-supplied name if sID=1 or 2
    link VARCHAR(255) COLLATE utf8mb4_general_ci UNIQUE NOT NULL,  -- doi (after https://doi.org/), url links (starting with http)
    FOREIGN KEY (dID) REFERENCES Documents(dID),
    FOREIGN KEY (sID) REFERENCES ExternalSources(sID),
    UNIQUE (dID, sID)
);

CREATE TABLE DocPhases (   -- statistics for past phases and overall numbers
    dID BIGINT UNSIGNED NOT NULL,
    nphase TINYINT UNSIGNED NOT NULL,    -- phase#: 0=overall, 1,2,3... = past phases
    reset_time DATETIME NOT NULL,        -- starting time of this phase
    Ntot_invited TINYINT UNSIGNED NOT NULL,  -- total# of invited reviews during this phase, not all docs required
    Ntot_contributed TINYINT UNSIGNED NOT NULL, -- total# of contributed reviews during this phase, not all required
    Ntot_comments INT UNSIGNED NOT NULL, -- total# of creditable comments during this phase
    Ntot_ratings INT UNSIGNED NOT NULL,  -- total# of ratings during this phase
    S_ave DOUBLE NOT NULL,               -- average rating (Aj for o-doc; Asyn for a-doc, <S> for others)
    ECP DOUBLE NOT NULL,                 -- ECP of this doc
    FOREIGN KEY (dID) REFERENCES Documents(dID),
    PRIMARY KEY (dID,nphase)
);

CREATE TABLE DocBranches (    -- each doc can list in (e.g., 3) research branches in general, but exceptions can be made
    dID BIGINT UNSIGNED NOT NULL,
    bID INT UNSIGNED NOT NULL,    -- must be a subfield or area (e.g., Phys.NP.nucl-ex) for a/o-doc
    num TINYINT UNSIGNED NOT NULL default 1,         -- order of the list of 3 branches
    impact TINYINT UNSIGNED NOT NULL default 100,    -- (1-100) impact percentage points used for ECP allocation to this branch. Total of all three should be 100.
    ts DATETIME DEFAULT CURRENT_TIMESTAMP,  -- starting time to show up in this branch's new-doc list
    FOREIGN KEY (bID) REFERENCES ResearchBranches(bID),
    FOREIGN KEY (dID) REFERENCES Documents(dID),
    INDEX (bID, ts, dID),
    PRIMARY KEY (dID, num)
);
-- e.g., INSERT INTO DocBranches (dID,num,bID,impact) VALUES (1,1,2,33) ON DUPLICATE KEY UPDATE bID = VALUES(bID), impact = VALUES(impact);
-- all 3 branches should be added or updated at the same time; no ts update; all three should use the ts of num=1.

CREATE TABLE DocTopics (    -- each doc can have only one topic or its sublevel
    dID BIGINT UNSIGNED PRIMARY KEY,
    tID INT UNSIGNED NOT NULL,    -- must be a topic / focus / question ID
    ts DATETIME DEFAULT CURRENT_TIMESTAMP,  -- starting time to show up in this topic's new-doc list
    FOREIGN KEY (dID) REFERENCES Documents(dID),
    FOREIGN KEY (tID) REFERENCES ResearchTopics(tID),
    INDEX (tID,dID)
);
-- e.g., INSERT INTO DocTopics (dID,tID) VALUES (1,2) ON DUPLICATE KEY UPDATE tID = VALUES(tID);

CREATE TABLE ModeratedDocs (   -- mod committee doesn't have to fill S_mod if they found no evidence to make a decision; this score (unless it is NULL) should override the one in the main table that is rated by members; another committee can modify it later on
    dID BIGINT UNSIGNED NOT NULL,
    comm_ID INT UNSIGNED NOT NULL,
    S_mod DOUBLE,    -- NULL means no evidence for a decision; otherwise typically a negative score between -1 and 0 depending on the fraudulent extent
    ts DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (dID) REFERENCES Documents(dID),
    FOREIGN KEY (comm_ID) REFERENCES Committees(ID),
    PRIMARY KEY (dID, comm_ID)
);

CREATE TABLE mwDocs ( -- extra information for m/w-docs, final report submitted by the committee
    dID BIGINT UNSIGNED PRIMARY KEY,
    comm_ID INT UNSIGNED NOT NULL,
    FOREIGN KEY (dID) REFERENCES Documents(dID),
    FOREIGN KEY (comm_ID) REFERENCES Committees(ID),
    INDEX (comm_ID)
);

CREATE TABLE vDocs ( -- extra information for v-docs
    dID BIGINT UNSIGNED PRIMARY KEY,
    comm_ID INT UNSIGNED NOT NULL,
    deadline DATETIME NOT NULL,
    eligibility TEXT COLLATE utf8mb4_general_ci, -- requirements on who can be a candidate (ECP,AL,bID,mrole), forming a mysql query string, or a fixed list
    vote_right TEXT COLLATE utf8mb4_general_ci,  -- requirements on who can vote (ECP,AL,bID,mrole), forming a mysql query string, this committee only, or other selected list of member IDs.
    FOREIGN KEY (dID) REFERENCES Documents(dID),
    FOREIGN KEY (comm_ID) REFERENCES Committees(ID),
    INDEX (comm_ID)
);

CREATE TABLE vDocWinners ( -- winner results of a v-doc
    dID BIGINT UNSIGNED NOT NULL,
    mID BIGINT UNSIGNED NOT NULL,
    place VARCHAR(60) COLLATE utf8mb4_general_ci,  -- NULL if only 1 winner, otherwise describing winning place (e.g., 2nd, 3rd, co-winner, etc.)
    FOREIGN KEY (dID) REFERENCES vDocs(dID),
    FOREIGN KEY (mID) REFERENCES Members(mID),
    UNIQUE (dID, mID)
);

CREATE TABLE vDocCandidates ( -- nominated candidates to be voted on
    dID BIGINT UNSIGNED NOT NULL,
    candidate BIGINT UNSIGNED NOT NULL,    -- possible for voters to nominate more candidates
    FOREIGN KEY (dID) REFERENCES vDocs(dID),
    FOREIGN KEY (candidate) REFERENCES Members(mID),
    PRIMARY KEY (dID, candidate)
);

CREATE TABLE vDocVotes ( -- votes on v-docs
    dID BIGINT UNSIGNED NOT NULL,
    voter BIGINT UNSIGNED NOT NULL,
    candidate BIGINT UNSIGNED NOT NULL,       -- possible to vote for multiple candidates
    ts DATETIME DEFAULT CURRENT_TIMESTAMP,    -- can not pass the deadline
    FOREIGN KEY (dID) REFERENCES vDocs(dID),
    FOREIGN KEY (voter) REFERENCES Members(mID),
    FOREIGN KEY (candidate) REFERENCES Members(mID),
    PRIMARY KEY (dID, candidate, voter)
);
-- each voting participation worth one ECP point (for one v-doc) by default

CREATE TABLE aDocList ( -- list of o/r-doc and their Aj for a given a-doc
    aID BIGINT UNSIGNED NOT NULL,
    oID BIGINT UNSIGNED NOT NULL,
    Aij DOUBLE NOT NULL,      -- each individual average score Aij
    FOREIGN KEY (aID) REFERENCES Documents(dID),
    FOREIGN KEY (oID) REFERENCES Documents(dID),
    PRIMARY KEY (aID, oID)
);

CREATE TABLE DocALS (   -- achievement level score for each a/o-doc in CURRENT PHASE for a given author
    mID BIGINT UNSIGNED NOT NULL,
    dID BIGINT UNSIGNED NOT NULL,
    Aij DOUBLE NOT NULL,          -- A (i=author, j=doc), Asyn for a-doc
    ALS DOUBLE NOT NULL,          -- alpha value = 6^Aij - 1 if positive, 1-6^(-3Aij) if negative
    FOREIGN KEY (dID) REFERENCES Documents(dID),
    FOREIGN KEY (mID) REFERENCES Members(mID),
    PRIMARY KEY (mID, dID)
);
-- o-doc in aDocList (or grouped in a-doc) should be removed from this table

CREATE TABLE OverallDocALS (   -- achievement level score for each a/o-doc (OVERALL) for a given author, only fill if dID is multi-phased, should not be used in general
    mID BIGINT UNSIGNED NOT NULL,
    dID BIGINT UNSIGNED NOT NULL,
    Aij DOUBLE NOT NULL,          -- A (i=author, j=doc)
    ALS DOUBLE NOT NULL,          -- alpha value = 6^Aij - 1 if positive, 1-6^(-3Aij) if negative
    FOREIGN KEY (dID) REFERENCES Documents(dID),
    FOREIGN KEY (mID) REFERENCES Members(mID),
    PRIMARY KEY (mID, dID)
);
-- o-doc in aDocList (or grouped in a-doc) should be removed from this table

CREATE TABLE BranchALS (   -- achievement level score for each branch in CURRENT PHASE for a given author
    mID BIGINT UNSIGNED NOT NULL,
    bID INT UNSIGNED NOT NULL,    -- Aijk, k denotes branches
    ALS DOUBLE NOT NULL,          -- alpha value = sum_j 6^Aijk - 1 if positive, 1-6^(-3Aijk) if negative
    FOREIGN KEY (bID) REFERENCES ResearchBranches(bID),
    FOREIGN KEY (mID) REFERENCES Members(mID),
    PRIMARY KEY (mID, bID)
); -- represent influence of the author in each research branch

CREATE TABLE OverallBranchALS (   -- achievement level score for each branch (OVERALL) for a given author, only fill if OverallDocALS is not empty for mID, should not be used in general
    mID BIGINT UNSIGNED NOT NULL,
    bID INT UNSIGNED NOT NULL,    -- Aijk, k denotes branches
    ALS DOUBLE NOT NULL,          -- alpha value = sum_j 6^Aijk - 1 if positive, 1-6^(-3Aijk) if negative
    FOREIGN KEY (bID) REFERENCES ResearchBranches(bID),
    FOREIGN KEY (mID) REFERENCES Members(mID),
    PRIMARY KEY (mID, bID)
); -- represent influence of the author in each research branch

CREATE TABLE ECPPartials (   -- partial ECP scores (CURRENT PHASE) for each member for a given research branch and doc_type
    mID BIGINT UNSIGNED NOT NULL,
    bID INT UNSIGNED NOT NULL,
    doctype TINYINT UNSIGNED NOT NULL,
    ECP DOUBLE NOT NULL,
    FOREIGN KEY (mID) REFERENCES Members(mID),
    FOREIGN KEY (bID) REFERENCES ResearchBranches(bID),
    FOREIGN KEY (doctype) REFERENCES DocTypes(ID),
    PRIMARY KEY (mID, bID, doctype)
);

CREATE TABLE OverallECPPartials (   -- partial ECP scores (OVERALL) for each member for a given research branch and doc_type, should not be used in general
    mID BIGINT UNSIGNED NOT NULL,
    bID INT UNSIGNED NOT NULL,
    doctype TINYINT UNSIGNED NOT NULL,
    ECP DOUBLE NOT NULL,
    FOREIGN KEY (mID) REFERENCES Members(mID),
    FOREIGN KEY (bID) REFERENCES ResearchBranches(bID),
    FOREIGN KEY (doctype) REFERENCES DocTypes(ID),
    PRIMARY KEY (mID, bID, doctype)
);


-- Comment-related tables

CREATE TABLE AnonymityTypes (
    ID TINYINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    atname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
    f_o DOUBLE NOT NULL,     -- the openness factor for ECP of derived contributions
--    is_active TINYINT(1) NOT NULL default 1,  -- 0 means a decommissioned type
    detail TEXT COLLATE utf8mb4_general_ci
);
INSERT INTO AnonymityTypes (ID,atname,f_o,detail) VALUES (1,'Open',1,'full credit');
INSERT INTO AnonymityTypes (ID,atname,f_o,detail) VALUES (2,'Open After Semi-Anonymous',0.5,'half credit');
INSERT INTO AnonymityTypes (ID,atname,f_o,detail) VALUES (3,'Semi-Anonymous',0.5,'half credit');
INSERT INTO AnonymityTypes (ID,atname,f_o,detail) VALUES (4,'Open After Anonymous',0.25,'quarter credit');
INSERT INTO AnonymityTypes (ID,atname,f_o,detail) VALUES (5,'Anonymous',0.25,'quarter credit');

CREATE TABLE CommentTypes (
    ID TINYINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    ctname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,    -- "Creditable Comment"
    abbr VARCHAR(2) COLLATE ascii_general_ci UNIQUE NOT NULL, -- "i" for invited review, "r" for contributed review, "c" for creditable comment, "u" for uncredited comment
    detail TEXT COLLATE utf8mb4_general_ci   -- optional detailed description
);
INSERT INTO CommentTypes (ID,ctname,abbr) VALUES (1,'Invited Review','i');
INSERT INTO CommentTypes (ID,ctname,abbr) VALUES (2,'Contributed Review','r');
INSERT INTO CommentTypes (ID,ctname,abbr) VALUES (3,'Creditable Comment','c');
INSERT INTO CommentTypes (ID,ctname,abbr) VALUES (4,'Uncredited Comment','u');

CREATE TABLE Comments (           -- submitter can not modify it any more, including uncredited comments
    cID BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    ctype TINYINT UNSIGNED NOT NULL DEFAULT 3, -- 3=creditable comment (default); 2=contributed review; 1=invited review; 4=uncredited comment
    parent_ID BIGINT UNSIGNED,    -- parent ID of either another comment or NULL if no parent
    dID BIGINT UNSIGNED NOT NULL,              -- Foreign key to the ultimate parent primary Document
    submitter_ID BIGINT UNSIGNED,     -- Submitter of the comment, NULL if anonymous (only for ctype 3/4)
    visibility TINYINT UNSIGNED NOT NULL default 60,   -- 99=visible to submitter/authors only (for draft) , 1=visible to all, 10=visible to members, 20=visible to raters+, 30=visible to reviewers+, 40=visible to moderators+, 50=visible to leaders+, 100=visible to administrators only; default to 60=ready to be announced.
    ts DATETIME DEFAULT CURRENT_TIMESTAMP,
    comment_text TEXT COLLATE utf8mb4_general_ci NOT NULL,
    -- ... more metadata
    FOREIGN KEY (ctype) REFERENCES CommentTypes(ID),
    FOREIGN KEY (dID) REFERENCES Documents(dID),
    FOREIGN KEY (submitter_ID) REFERENCES Members(mID),
    FOREIGN KEY (visibility) REFERENCES VisibilityTypes (ID),
    FOREIGN KEY (parent_ID) REFERENCES Comments(cID), -- Self-referencing for replies
    INDEX (dID, ctype, visibility),
    INDEX (parent_ID, ctype, visibility),
    INDEX (submitter_ID),
    FULLTEXT INDEX (comment_text)
);
-- e.g., INSERT INTO Comments (ctype,parent_ID,dID,submitter_ID,comment_text) VALUES (...);
-- additional insert for creditable: INSERT INTO CRComments (cID,ID_num,inviter_id,anonymity,Nth,T)
-- if submitter_ID is NULL, no edit from the anonymous submitter is allowed

CREATE TABLE CRComments (      -- extra information for creditable comments / contributed or invited reviews; to be left joined to 'Comments'
    cID BIGINT UNSIGNED PRIMARY KEY,
    ID_num INT UNSIGNED NOT NULL default 1,       -- sequential # under the same doc dID
    ID_alphanum VARCHAR(4) COLLATE ascii_general_ci NOT NULL default (CONV(ID_num,10,36)),
    inviter_id BIGINT UNSIGNED NULL,           -- ID of the inviter if it is an invited review
    author_list JSON,        -- JSON array of authors without affiliations: [[author_name=pub_name, mID , significance_factor=duty], ...] (author list in order, editors for c/m/w docs, organizers for f/s/t docs), null means submitter is the sole author  -- more complex: {"authors" : [[pub_name, mID, duty, [list of order numbers of affiliations]], ...], "affiliations": [[order#, name], ...]}
    anonymity TINYINT UNSIGNED default 1,      -- anonymity level: 1='open', 2='open-after-semi-anonym', 3='semi-anonymous', 4='open-after-anonym', 5='anonymous'
    passcode VARCHAR(255) COLLATE utf8mb4_general_ci,     -- needed only if anonymity_level=4
    Nth INT UNSIGNED NOT NULL default 1,   -- this is the N-th comment/review in current phase, set to 0 if phase is changed
    T INT UNSIGNED NOT NULL default 0,     -- time lapse in the current or old phase in units of minutes
    N_ratings INT UNSIGNED NOT NULL DEFAULT 0,      -- number of ratings received in the current phase
    S_ave DOUBLE NOT NULL DEFAULT 0,                -- Average rating score for current phase
    ECP DOUBLE NOT NULL DEFAULT 0,                  -- ECP of this comment/review
    -- ... more metadata
--    UNIQUE (dID, ID_alphanum),
--    UNIQUE (dID, ID_num),
    CONSTRAINT CHK_passcode CHECK (anonymity < 4 OR passcode IS NOT NULL),
    FOREIGN KEY (cID) REFERENCES Comments (cID),
    FOREIGN KEY (inviter_id) REFERENCES Members (mID),
    FOREIGN KEY (anonymity) REFERENCES AnonymityTypes (ID),
    INDEX (ID_num, ID_alphanum, anonymity),
    INDEX (inviter_id)
);

CREATE TABLE CommentDrafts (  -- submitter can continue to modify it; for creditable comments/reviews; the draft will be deleted once it is published
    cID BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    ctype TINYINT UNSIGNED NOT NULL DEFAULT 3, -- 3=creditable comment; 2=contributed review; 1=invited review; 4=uncredited comment
    parent_ID BIGINT UNSIGNED,    -- parent ID of either another comment or NULL if no parent
    dID BIGINT UNSIGNED NOT NULL,              -- Foreign key to the ultimate parent primary Document
    submitter_ID BIGINT UNSIGNED, -- Submitter of the comment, NULL if anonymous (anonymity=5 only)
    inviter_id BIGINT UNSIGNED,   -- ID of the inviter if it is an invited review
    author_list JSON,        -- JSON array of authors without affiliations: [[author_name=pub_name, mID , significance_factor=duty], ...] (author list in order, editors for c/m/w docs, organizers for f/s/t docs), null means submitter is the sole author  -- more complex: {"authors" : [[pub_name, mID, duty, [list of order numbers of affiliations]], ...], "affiliations": [[order#, name], ...]}
    ts DATETIME DEFAULT CURRENT_TIMESTAMP,
    comment_text TEXT COLLATE utf8mb4_general_ci NOT NULL,
    anonymity TINYINT UNSIGNED default 1,      -- anonymity level: 1='open', 2='open-after-semi-anonym', 3='semi-anonymous', 4='open-after-anonym', 5='anonymous'
    passcode VARCHAR(255) COLLATE utf8mb4_general_ci NULL,     -- needed only if anonymity_level=4
    to_be_moderated TINYINT(1) NOT NULL default 0,    -- set to 1 if anonymous / untrustable submitter click 'submit'  -- use last timestamp to determine N / T values if approved and moved to the Comments table
    -- ... more metadata
    CONSTRAINT CHK_passcode CHECK (anonymity < 4 OR passcode IS NOT NULL),
    CONSTRAINT CHK_submitterID CHECK (anonymity = 5 OR submitter_ID IS NOT NULL),
    FOREIGN KEY (ctype) REFERENCES CommentTypes(ID),
    FOREIGN KEY (dID) REFERENCES Documents(dID),
    FOREIGN KEY (submitter_ID) REFERENCES Members(mID),
    FOREIGN KEY (inviter_id) REFERENCES Members(mID),
    FOREIGN KEY (parent_ID) REFERENCES Comments(cID),
    FOREIGN KEY (anonymity) REFERENCES AnonymityTypes (ID),
    INDEX (dID,ctype,anonymity),
    INDEX (parent_ID,ctype,anonymity),
    INDEX (submitter_ID,inviter_id)
); -- if submitter_ID is NULL (anonymity = 5) or not in good standing, it should be under moderation automatically when the submit button is clicked (to_be_moderated set to 1 and nothing is submitted to 'Comments', a moderator will check manually).

CREATE TABLE CRCommentPhases (   -- saved past phases and overall numbers
    cID BIGINT UNSIGNED NOT NULL,
    nphase TINYINT UNSIGNED NOT NULL,     -- phase#: 0=overall, 1,2,3... = past phases
    Nth INT UNSIGNED NOT NULL,            -- the N-th comment/review in this phase
    T INT UNSIGNED NOT NULL,              -- time lapse in this phase in units of minutes
    N_ratings INT UNSIGNED NOT NULL,      -- number of ratings received in this phase
    S_ave DOUBLE NOT NULL,                -- Average rating score for this phase
    ECP DOUBLE NOT NULL,                  -- ECP of this comment/review
    FOREIGN KEY (cID) REFERENCES CRComments(cID),
    PRIMARY KEY (cID,nphase)
);


-- Doc/Comment and Authors cross-links

CREATE TABLE DocAuthors (   -- authors with no mID will not be recorded here
    dID BIGINT UNSIGNED NOT NULL,
    mID BIGINT UNSIGNED NOT NULL,
--    author_order INT UNSIGNED,                   -- Order of authors in the list
    duty TINYINT UNSIGNED NOT NULL DEFAULT 100,  -- Significance percentage in contribution normalized to 100% of 1st-class
    frac DOUBLE NOT NULL default 1,  -- fraction for ECP calculation = duty / sum of all duties (including general classes)
--    author_class TINYINT UNSIGNED,   -- 1='1st-author', 2='2nd-author', 3='3rd-author', 4='variable-author', 5='general-author': classified if<5; non-classified if>=5 (won't count as duty cap)
    contribution VARCHAR(255) COLLATE utf8mb4_general_ci,
    -- ... additional fields (specific contribution details if needed)
    FOREIGN KEY (dID) REFERENCES Documents(dID),
    FOREIGN KEY (mID) REFERENCES Members(mID),
--    FOREIGN KEY (author_class) REFERENCES AuthorClasses (ID),
    PRIMARY KEY (dID, mID),         -- Ensure no duplicate authors for the same document
--    UNIQUE (dID,author_order),
    INDEX (mID)
);
-- e.g., INSERT INTO DocAuthors (dID,mID,duty,frac) VALUES (1,2,100,0.5) ON DUPLICATE KEY UPDATE duty=VALUES(duty), frac=VALUES(frac);

CREATE TABLE CommentAuthors (   -- authors with no mID will not be recorded here
    cID BIGINT UNSIGNED NOT NULL,
    mID BIGINT UNSIGNED NOT NULL,
    duty TINYINT UNSIGNED NOT NULL DEFAULT 100,  -- Significance percentage in variable scheme (20-100)%
    frac DOUBLE NOT NULL default 1,     -- fraction for ECP calculation = duty / sum of all duties
    contribution VARCHAR(255) COLLATE utf8mb4_general_ci,
    -- ... additional fields (specific contribution details if needed)
    FOREIGN KEY (cID) REFERENCES CRComments(cID),
    FOREIGN KEY (mID) REFERENCES Members(mID),
    PRIMARY KEY (cID, mID),         -- Ensure no duplicate authors for the same comment/review
    INDEX (mID)
);
-- e.g., INSERT INTO CommentAuthors (cID,mID,duty,frac) VALUES (1,2,50,0.2) ON DUPLICATE KEY UPDATE duty=VALUES(duty),frac=VALUES(frac);

CREATE TABLE CommentDraftAuthors (   -- co-authors who can edit/approve CommentDrafts; but only submitter can submit creditable comments/reviews
    cID BIGINT UNSIGNED NOT NULL,
    mID BIGINT UNSIGNED NOT NULL,
    is_approved TINYINT(1) NOT NULL default 0, -- 1 means the author approved it
    is_locked TINYINT(1) NOT NULL default 0,   -- 1 means the author's choice is locked no matter any future changes (completely trust the submitter if it is approval; completely refute the item if it is disapproval); 0 means any future update needs the author's re-approval.
    FOREIGN KEY (cID) REFERENCES CommentDrafts(cID) ON DELETE CASCADE,
    FOREIGN KEY (mID) REFERENCES Members(mID),
    PRIMARY KEY (cID, mID),         -- Ensure no duplicate authors for the same item
    INDEX (cID, is_approved)
); -- only for creditable comments and reviews (can not be completely anonymous)


-- Rating-related tables

CREATE TABLE DocRatings (       -- for rating parent documents
    rID BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    dID BIGINT UNSIGNED NOT NULL,                -- ID of the item being rated (parent doc ID)
    rater_ID BIGINT UNSIGNED NOT NULL,           -- Rater member_ID
    num TINYINT UNSIGNED NOT NULL default 1,     -- the n-th rating by the same user on the same doc (maximum 3 times)
    ts DATETIME DEFAULT CURRENT_TIMESTAMP,
    score TINYINT NOT NULL,      -- Rating score (-10 to 50 corresponding to -1.0 to 5.0)
    pscore1 TINYINT,             -- partial score1: scientific soundness score for p-doc 
    pscore2 TINYINT,             -- partial score2: potential impact score for p-doc 
    pscore3 TINYINT,             -- partial score3: technical feasibility score for p-doc 
--    rating_type TINYINT NOT NULL default 1,    -- 1 with no creditable contribution, 2= with creditable comments, 3=with contributed reviews, 4=with invited reivew
    Nth INT UNSIGNED NOT NULL default 0,  -- the N-th rating in current phase, 0=not calculated yet
    T INT UNSIGNED NOT NULL default 0,    -- time lapse in the current phase in units of minutes, 0=not calculated yet
    ECP DOUBLE NOT NULL DEFAULT 0,        -- ECP of this rating
    -- ... metadata about the rating context (e.g., reason for rating)
    FOREIGN KEY (dID) REFERENCES Documents(dID),
    FOREIGN KEY (rater_ID) REFERENCES Members(mID),
    UNIQUE (dID, rater_ID, num),
    INDEX (rater_ID)                             -- Index for user's rating history
);
-- e.g., select only the last vote from each member
-- WITH ranked_Ratings AS (
--   SELECT m.*, ROW_NUMBER() OVER (PARTITION BY rater_ID ORDER BY num DESC) AS rn
--   FROM DocRatings AS m WHERE dID = xxxx
-- )
-- SELECT * FROM ranked_Ratings WHERE rn = 1;
-- 
-- e.g., INSERT INTO DocRatings (dID, rater_ID, num, score, pscore1, pscore2, pscore3, Nth, T) VALUES (...);

CREATE TABLE CommentRatings (            -- for rating comments / reviews
    rID BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    cID BIGINT UNSIGNED NOT NULL,                -- ID of the item being rated (comment_ID)
    rater_ID BIGINT UNSIGNED NOT NULL,           -- Rater member_ID
    num TINYINT UNSIGNED NOT NULL default 1,     -- the n-th rating by the same user on the same comment/review (maximum 3 times) or maybe only allow one time
    ts DATETIME DEFAULT CURRENT_TIMESTAMP,
    score TINYINT NOT NULL,                      -- Rating score (-10 to 50 corresponding to -1.0 to 5.0)
--    rating_type TINYINT NOT NULL default 1,    -- 1 with no creditable contribution, 2= with creditable comments, 3=with contributed reviews, 4=with invited reivew
    Nth INT UNSIGNED NOT NULL default 0,      -- the N-th rating in current phase, 0=not calculated yet
    T INT UNSIGNED NOT NULL default 0,        -- time lapse in the current phase in units of minutes, 0=not calculated yet
    ECP DOUBLE NOT NULL DEFAULT 0,               -- ECP of this rating
    -- ... metadata about the rating context (e.g., reason for rating)
    FOREIGN KEY (cID) REFERENCES Comments(cID),
    FOREIGN KEY (rater_ID) REFERENCES Members(mID),
    UNIQUE (cID, rater_ID, num),
    INDEX (rater_ID)                             -- Index for user's rating history
);

CREATE TABLE DocRatingPhases (   -- saved past phases and overall numbers
    rID BIGINT UNSIGNED NOT NULL,
    nphase TINYINT UNSIGNED NOT NULL,     -- phase#: 0=overall, 1,2,3... = past phases
    Nth INT UNSIGNED NOT NULL,            -- the N-th rating in this phase
    T INT UNSIGNED NOT NULL,              -- time lapse in this phase in units of minutes
    ECP DOUBLE NOT NULL,                  -- ECP of this rating
    FOREIGN KEY (rID) REFERENCES DocRatings(rID),
    PRIMARY KEY (rID,nphase)
);

CREATE TABLE CommentRatingPhases (   -- saved past phases and overall numbers
    rID BIGINT UNSIGNED NOT NULL,
    nphase TINYINT UNSIGNED NOT NULL,     -- phase#: 0=overall, 1,2,3... = past phases
    Nth INT UNSIGNED NOT NULL,            -- the N-th rating in this phase
    T INT UNSIGNED NOT NULL,              -- time lapse in this phase in units of minutes
    ECP DOUBLE NOT NULL,                  -- ECP of this rating
    FOREIGN KEY (rID) REFERENCES CommentRatings(rID),
    PRIMARY KEY (rID,nphase)
);


CREATE TABLE SrevList ( -- the average score*duty% of the review whose ECP calculations under the same doc are based on
    dID BIGINT UNSIGNED NOT NULL,
    mID BIGINT UNSIGNED NOT NULL,
    srev DOUBLE NOT NULL,      -- <S> * duty% of the member or the AL allocation method
    invited TINYINT(1) NOT NULL default 0,   -- 1=invited reivew, 0=contributed review, can only do one of the two
    FOREIGN KEY (dID) REFERENCES Documents(dID),
    FOREIGN KEY (mID) REFERENCES Members(mID),
    PRIMARY KEY (dID, mID)
);

CREATE TABLE SmaxList ( -- max score*duty% of creditable comments whose ECP calculations under the same doc are based on
    dID BIGINT UNSIGNED NOT NULL,
    mID BIGINT UNSIGNED NOT NULL,
    cID BIGINT UNSIGNED NOT NULL,
    smax DOUBLE NOT NULL,  -- <S>_max * duty% of the member or the AL allocation method
    FOREIGN KEY (dID) REFERENCES Documents(dID),
    FOREIGN KEY (cID) REFERENCES CRComments(cID),
    FOREIGN KEY (mID) REFERENCES Members(mID),
    PRIMARY KEY (dID, mID,cID)
);
-- e.g. not working well: INSERT INTO Smax (dID,mID,smax) VALUES (1,2,3) ON DUPLICATE KEY UPDATE smax = GREATEST(smax, VALUES(smax));
-- e.g. INSERT INTO Srev (dID,mID,srev,invited) VALUES (1,2,3,0) ON DUPLICATE KEY UPDATE srev = VALUES(srev), invited = VALUES(invited);
-- e.g. INSERT INTO Smax (dID,mID,smax) VALUES (1,2,3) ON DUPLICATE KEY UPDATE smax = VALUES(smax);
  

-- Flag-related tables (including collections)

CREATE TABLE COIRelationships (   -- potential COI (conflict of interest) relationships
    ID TINYINT UNSIGNED PRIMARY KEY,
    coiname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
    strict_order TINYINT(1) NOT NULL default 0,   -- 1=preserve the order of mID1 and mID2
    is_active TINYINT(1) NOT NULL default 1,      -- 0 means a decommissioned relationship
    detail TEXT COLLATE utf8mb4_general_ci,
    INDEX (coiname, is_active)
);
-- COI checks work mostly in background for funding/rating related activities, visible only to related members, not to general public and other members; if COI later revealed, corresponding ratings, etc., will be removed, reviews/creditable comments will be converted to uncredited comments.
INSERT INTO COIRelationships (ID,coiname,strict_order,detail) VALUES (1,'PhD Advisor',1,'a strict relatioinship');
INSERT INTO COIRelationships (ID,coiname,strict_order,detail) VALUES (2,'Postdoc Advisor',1,'a strict relatioinship');
INSERT INTO COIRelationships (ID,coiname,strict_order,detail) VALUES (3,'Supervisor (Other Types)',1,'other than PhD or Postdoc Advisor');
INSERT INTO COIRelationships (ID,coiname,strict_order,detail) VALUES (4,'Family',0,'spouse/parent/child/sibling or anyone living together');
INSERT INTO COIRelationships (ID,coiname,strict_order,detail) VALUES (5,'Relative',0,'cousin, uncle, aunt, niece, nephew, grand-*, in-laws');
INSERT INTO COIRelationships (ID,coiname,strict_order,detail) VALUES (6,'Personal',0,'close friends');
INSERT INTO COIRelationships (ID,coiname,strict_order,detail) VALUES (7,'Business',0,'partner, financial interest');
INSERT INTO COIRelationships (ID,coiname,strict_order,detail) VALUES (8,'co-Editor',0,'work for the same journal/publication entity');
INSERT INTO COIRelationships (ID,coiname,strict_order,detail) VALUES (9,'Colleague',0,'work in the same place (department)');
INSERT INTO COIRelationships (ID,coiname,strict_order,detail) VALUES (10,'Collaborator',0,'collaboration in research');
INSERT INTO COIRelationships (ID,coiname,strict_order,detail) VALUES (11,'co-Author',0,'general-class does not count');
INSERT INTO COIRelationships (ID,coiname,strict_order,detail) VALUES (255,'Other COI Relationship',0,'everything else');

CREATE TABLE COIFlags (      -- COI (conflict of interest) flags
    mID1 BIGINT UNSIGNED NOT NULL,    -- mID1 is xxxx to mID2, typically mID1 < mID2 unless strick_order=1 (advisor)
    mID2 BIGINT UNSIGNED NOT NULL,
    relationship TINYINT UNSIGNED NOT NULL,  -- 1-3=advisor, 4=family, 5=relative, 6=personal, 7=business, 8=co-editor, 9=colleague, 10=collaborator, 11=coauthor, 255=other COI relationship
    is_active TINYINT(1) NOT NULL default 1, -- 0 means an inactive COI if mID1 or mID2 is inactive
    start_date DATE,  -- NULL means permanent
    end_date DATE,    -- NULL means no end date
    FOREIGN KEY (relationship) REFERENCES COIRelationships(ID),
    FOREIGN KEY (mID1) REFERENCES Members(mID),
    FOREIGN KEY (mID2) REFERENCES Members(mID),
    PRIMARY KEY (mID1,mID2,relationship),
    INDEX (mID1,mID2,is_active)
);  -- expired relationship should be deleted (including deceased)
-- unless relationship=1 or similar that restricts the order, mID1 < mID2 is required to ensure UNIQUE constraint
-- ON DUPLICATE KEY UPDATE start_date = VALUES(start_date), end_date = VALUES(end_date);

CREATE TABLE MemberRelationships (   -- user-applied relationships between members
    ID TINYINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    mrname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
    is_active TINYINT(1) NOT NULL default 1,      -- 0 means a decommissioned relationship
    detail TEXT COLLATE utf8mb4_general_ci,
    INDEX (mrname,is_active)
);
INSERT INTO MemberRelationships (ID,mrname,detail) VALUES (1,'Stars','most admired/respected/loved');
INSERT INTO MemberRelationships (ID,mrname,detail) VALUES (2,'Friends','close friends for sharing');
INSERT INTO MemberRelationships (ID,mrname,detail) VALUES (3,'Follow','to follow their work');
INSERT INTO MemberRelationships (ID,mrname,detail) VALUES (4,'Block','to block their posts');

CREATE TABLE MemberFlags (  -- stars, friends, follow, block, ...
    mID1 BIGINT UNSIGNED NOT NULL,    -- the owner of the flag
    mID2 BIGINT UNSIGNED NOT NULL,    -- to be flagged
    relationship TINYINT UNSIGNED NOT NULL,  -- 1=stars, 2=friends, 3=follow, 4=block
    FOREIGN KEY (relationship) REFERENCES MemberRelationships(ID),
    FOREIGN KEY (mID1) REFERENCES Members(mID),
    FOREIGN KEY (mID2) REFERENCES Members(mID),
    PRIMARY KEY (mID1,mID2,relationship)
);

CREATE TABLE FlagTypes (   -- user-applied tags on documents and comments/reviews
    ID TINYINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    ftname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
    is_active TINYINT(1) NOT NULL default 1,      -- 0 means a decommissioned flag type
    detail TEXT COLLATE utf8mb4_general_ci,
    INDEX (ftname,is_active)
);
INSERT INTO FlagTypes (ID,ftname,detail) VALUES (1,'Favorites','my favorites');
INSERT INTO FlagTypes (ID,ftname,detail) VALUES (2,'To Be Read','waiting list to be read');
INSERT INTO FlagTypes (ID,ftname,detail) VALUES (3,'Current Focus','related to my current project');

CREATE TABLE FraudTypes (   -- user-applied fraud-type tags on documents and comments/reviews, PUBLIC only
    ID TINYINT UNSIGNED PRIMARY KEY,
    ftname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
    is_active TINYINT(1) NOT NULL default 1,      -- 0 means a decommissioned flag type
    detail TEXT COLLATE utf8mb4_general_ci,
    INDEX (ftname,is_active)
);
-- ID=1-99 means detrimental, its visibility can be blocked for users below the role of who flags if the fagger chooses so. If a member flags, can make it invisible to public; if a rater flags, can make it invisible to general members; if a reviewer flags, can make it invisible to raters and below; if a moderator flags, can make it invisible to reviewers and below; if a leader flags, can make it invisible to moderators and below (in practice, more likely to choose to reviewers and below).
INSERT INTO FraudTypes (ID,ftname,detail) VALUES (1,'Fraudulent','fake, manipulated, etc.');
INSERT INTO FraudTypes (ID,ftname,detail) VALUES (2,'Plagiaristic','copycat');
INSERT INTO FraudTypes (ID,ftname,detail) VALUES (3,'Abusive','offensive, insulting language');
INSERT INTO FraudTypes (ID,ftname,detail) VALUES (99,'Harmful(in Other Ways)','detrimental in ways other than the above explicitly listed');
-- ID=100 means unvote the above detrimental flags (option available only if 1-99 flags exist)
INSERT INTO FraudTypes (ID,ftname,detail) VALUES (100,'Not Detrimental','veto other detrimental flags');
-- members can choose only one of the these fraud flags, can modify within two weeks. Option 100 should not appear unless there is at least a fraud tag from 1-99.

CREATE TABLE DocFlags (     -- favorites, to be read, current focus, etc.
    mID BIGINT UNSIGNED NOT NULL,   -- the owner of the flag
    dID BIGINT UNSIGNED NOT NULL,   -- to be flagged
    ftype TINYINT UNSIGNED NOT NULL,  -- 1=favorites, 2=to be read, 3=current focus
    FOREIGN KEY (dID) REFERENCES Documents(dID),
    FOREIGN KEY (mID) REFERENCES Members(mID),
    FOREIGN KEY (ftype) REFERENCES FlagTypes(ID),
    PRIMARY KEY (mID,ftype,dID)
);

CREATE TABLE DocFraudFlags (     -- fraud alert !!!
    mID BIGINT UNSIGNED NOT NULL,   -- the owner of the flag
    dID BIGINT UNSIGNED NOT NULL,   -- to be flagged
    ftype TINYINT UNSIGNED NOT NULL,  -- 1-99=detrimental, 100=not detrimental
    FOREIGN KEY (dID) REFERENCES Documents(dID),
    FOREIGN KEY (mID) REFERENCES Members(mID),
    FOREIGN KEY (ftype) REFERENCES FraudTypes(ID),
    PRIMARY KEY (dID,mID),
    INDEX (dID,ftype)
);
-- ON DUPLICATE KEY UPDATE ftype = VALUES(ftype)

CREATE TABLE CommentFlags (  -- favorites, to be read, current focus, etc.
    mID BIGINT UNSIGNED NOT NULL,   -- the owner of the flag
    cID BIGINT UNSIGNED NOT NULL,   -- to be flagged
    ftype TINYINT UNSIGNED NOT NULL,  -- 1=favorites, 2=to be read, 3=current focus
    FOREIGN KEY (cID) REFERENCES Comments(cID),
    FOREIGN KEY (mID) REFERENCES Members(mID),
    FOREIGN KEY (ftype) REFERENCES FlagTypes(ID),
    PRIMARY KEY (mID,ftype,cID)
);

CREATE TABLE CommentFraudFlags (    -- fraud alert !!!
    mID BIGINT UNSIGNED NOT NULL,   -- the owner of the flag
    cID BIGINT UNSIGNED NOT NULL,   -- to be flagged
    ftype TINYINT UNSIGNED NOT NULL,  -- 1-99=detrimental, 100=not detrimental
    FOREIGN KEY (cID) REFERENCES Comments(cID),
    FOREIGN KEY (mID) REFERENCES Members(mID),
    FOREIGN KEY (ftype) REFERENCES FraudTypes(ID),
    PRIMARY KEY (cID,mID),
    INDEX (cID,ftype)
);
-- ON DUPLICATE KEY UPDATE ftype = VALUES(ftype)

CREATE TABLE Collections (     -- user-named collections for parent documents
    ID BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    mID BIGINT UNSIGNED NOT NULL,    -- the owner of the flag
    cnum TINYINT UNSIGNED NOT NULL default 1,  -- collection # for this member (may set a limit)
    cname VARCHAR(255) COLLATE utf8mb4_general_ci NOT NULL,  -- collection name
    public TINYINT UNSIGNED NOT NULL default 0,   -- 0 = private, 1=share with friends, 2 = go public
    FOREIGN KEY (mID) REFERENCES Members(mID),
    UNIQUE (mID,cnum),
    INDEX (cname,public)
);

CREATE TABLE CollectionFlags (     -- flag docs to be included in user-named collections
    coID BIGINT UNSIGNED NOT NULL,    -- collection ID
    dID BIGINT UNSIGNED NOT NULL,     -- to be flagged
    FOREIGN KEY (dID) REFERENCES Documents (dID),
    FOREIGN KEY (coID) REFERENCES Collections (ID) ON DELETE CASCADE,
    PRIMARY KEY (coID,dID)
);

CREATE TABLE ShareDocFlags ( -- make flags(favorites/to be read/current focus) public, or sharing with others; automatically private if not listed here
    mID BIGINT UNSIGNED NOT NULL,    -- the owner of the flag
    ftype TINYINT UNSIGNED NOT NULL, -- doc-flags
    public TINYINT NOT NULL default 1,    -- 1 = share with friends, 2 = go public
    FOREIGN KEY (mID) REFERENCES Members(mID),
    FOREIGN KEY (ftype) REFERENCES FlagTypes(ID),
    PRIMARY KEY (mID,ftype)
);
CREATE TABLE ShareCommentFlags ( -- make flags(favorites/to be read/current focus) public, or sharing with others; automatically private if not listed here
    mID BIGINT UNSIGNED NOT NULL,    -- the owner of the flag
    ftype TINYINT UNSIGNED NOT NULL, -- comment flags
    public TINYINT NOT NULL default 1,    -- 1 = share with friends, 2 = go public
    FOREIGN KEY (mID) REFERENCES Members(mID),
    FOREIGN KEY (ftype) REFERENCES FlagTypes(ID),
    PRIMARY KEY (mID,ftype)
);


-- Member Organization-related tables (for potential sponsors and funding/hiring/support entities)

CREATE TABLE OrgTypes (  -- types of member organizations that sponsor CORE
    ID TINYINT UNSIGNED PRIMARY KEY,
    otname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
    is_active TINYINT(1) NOT NULL default 1,    -- 0 means a decommissioned type
    detail TEXT COLLATE utf8mb4_general_ci,
    INDEX (otname,is_active)
);
INSERT INTO OrgTypes (ID,otname,detail) VALUES (1,'Research Colleges (PhD granting)','Research-oriented');
INSERT INTO OrgTypes (ID,otname,detail) VALUES (2,'Teaching Colleges (non-PhD-granting)','Mostly for undergraduate education');
INSERT INTO OrgTypes (ID,otname,detail) VALUES (3,'K12 & Other Education Institutions','K12 schools, training schools, etc.');
INSERT INTO OrgTypes (ID,otname,detail) VALUES (4,'Government Laboratories','National or State-owned laboratories');
INSERT INTO OrgTypes (ID,otname,detail) VALUES (5,'Government Funding Agencies','national or state funding agencies');
INSERT INTO OrgTypes (ID,otname,detail) VALUES (6,'Private Foundations','prrivate funding orgs');
INSERT INTO OrgTypes (ID,otname,detail) VALUES (7,'Other Non-Profit Organizations','NPOs');
INSERT INTO OrgTypes (ID,otname,detail) VALUES (8,'University Departments/Laboratories','sub-unit in a university');
INSERT INTO OrgTypes (ID,otname,detail) VALUES (9,'Industrial Research Laboratories','funded by companies');
INSERT INTO OrgTypes (ID,otname,detail) VALUES (10,'Business Entities','Companies');
INSERT INTO OrgTypes (ID,otname,detail) VALUES (244,'Individuals','Any individual who likes to sponsor research');
INSERT INTO OrgTypes (ID,otname,detail) VALUES (255,'Other Entities','Any other organizations interested in research');

CREATE TABLE OrgTiers ( -- membership tiers for organizations
    ID TINYINT UNSIGNED PRIMARY KEY,
    tname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
--    is_active TINYINT(1) NOT NULL default 1,    -- 0 means a decommissioned type
    detail TEXT COLLATE utf8mb4_general_ci
);
INSERT INTO OrgTiers (ID,tname,detail) VALUES (1,'Green','entry-level / free for individuals and small groups who like to fund/support research');
INSERT INTO OrgTiers (ID,tname,detail) VALUES (2,'Standard','standard level with minimum fees');
INSERT INTO OrgTiers (ID,tname,detail) VALUES (3,'Bronze','low annual fees');
INSERT INTO OrgTiers (ID,tname,detail) VALUES (4,'Silver','medium annual fees');
INSERT INTO OrgTiers (ID,tname,detail) VALUES (5,'Gold','higher annual fees');
INSERT INTO OrgTiers (ID,tname,detail) VALUES (6,'Platinum','even higher annual fees');
INSERT INTO OrgTiers (ID,tname,detail) VALUES (7,'Diamond','highest fixed annual fees');
INSERT INTO OrgTiers (ID,tname,detail) VALUES (255,'Unlimited','Biggest (neogotiated) Sponsors');

CREATE TABLE MemberOrganizations (
    oID INT UNSIGNED PRIMARY KEY AUTO_INCREMENT,  -- unique identifier for each member organization
    otype TINYINT UNSIGNED NOT NULL,           -- 1=R-college, 2=T-college, 3=k12&other_edu, 4=national/state labs, 5=government funding agency, 6= private foundation, 7= NPOs, 8=univ-depts/labs, 9=Industrial labs, 10=companies, 255=other
    oname VARCHAR(255) COLLATE utf8mb4_general_ci NOT NULL,      -- "University of Notre Dame"
    abbr VARCHAR(30) COLLATE utf8mb4_general_ci UNIQUE NOT NULL, -- "UND"
    address VARCHAR(255) COLLATE utf8mb4_general_ci,             -- mailing address
    email VARCHAR(254) COLLATE utf8mb4_general_ci UNIQUE,
--    pass VARCHAR(255) NOT NULL,    -- password hash;
--    token VARCHAR(255),            -- for persistent logins; NULL represents an inactive user
    contact VARCHAR(255) COLLATE utf8mb4_general_ci,
    homepage VARCHAR(255) COLLATE utf8mb4_general_ci,
    tier TINYINT UNSIGNED NOT NULL default 1,      -- 1=green, 2=standard, 3==bronze, 4=silver, 5=gold, 6=platinum, 7=diamond depending on sponsorship level
    is_good TINYINT(1) NOT NULL default 1,         -- true for good standing (no fees overdue)
    is_active TINYINT(1) NOT NULL default 1,       -- true for active member
    detail TEXT COLLATE utf8mb4_general_ci,        -- Optional detailed description
    start_green DATETIME,
    start_standard DATETIME,
    start_bronze DATETIME,
    start_silver DATETIME,
    start_gold DATETIME,
    start_platinum DATETIME,
    start_diamond DATETIME,
    start_unlimited DATETIME,
    FOREIGN KEY (otype) REFERENCES OrgTypes (ID),
    FOREIGN KEY (tier) REFERENCES OrgTiers (ID),
    INDEX (otype,abbr,is_good,is_active),
    INDEX (tier,oname)
);
-- can log in with oID / abbr / email + password
-- need a separate login for these organizations

CREATE TABLE OrgRoleTypes (  -- role types of member organization managers
    ID TINYINT UNSIGNED PRIMARY KEY,
    rtname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
    is_active TINYINT(1) NOT NULL default 1,    -- 0 means a decommissioned type
    detail TEXT COLLATE utf8mb4_general_ci,
    INDEX (rtname,is_active)
);
INSERT INTO OrgRoleTypes (ID,rtname,detail) VALUES (1,'Stuff','basic level stuff');   -- view only
INSERT INTO OrgRoleTypes (ID,rtname,detail) VALUES (10,'Manager','program managers'); -- manage program/job
INSERT INTO OrgRoleTypes (ID,rtname,detail) VALUES (100,'Admin','Administrators');    -- assign the roles

CREATE TABLE OrgManagers (  -- managers of each member organization
    oID INT UNSIGNED NOT NULL,      -- ID of the organization
    mID BIGINT UNSIGNED NOT NULL,   -- member ID of the manager
    orole TINYINT UNSIGNED NOT NULL default 1,  -- role of the manager
    FOREIGN KEY (oID) REFERENCES MemberOrganizations (oID),
    FOREIGN KEY (mID) REFERENCES Members (mID),
    FOREIGN KEY (orole) REFERENCES OrgRoleTypes (ID),
    PRIMARY KEY (oID, mID)
);


-- Funding related tables

CREATE TABLE FundingTypes (
    ID TINYINT UNSIGNED PRIMARY KEY,
    ftname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
    doc_type TINYINT UNSIGNED default 255,     -- 255 means not related to any CORE docs
    is_active TINYINT(1) NOT NULL default 1,   -- 0 means a decommissioned flag type
    detail TEXT COLLATE utf8mb4_general_ci,
    FOREIGN KEY (doc_type) REFERENCES DocTypes(ID),
    INDEX (doc_type,is_active)
);
INSERT INTO FundingTypes (ID,ftname,doc_type,detail) VALUES (1,'Original Research (Mainstream)',9,'mainstream p-doc');
INSERT INTO FundingTypes (ID,ftname,doc_type,detail) VALUES (2,'Original Research (High-Risk)',9,'high-risk high-reward p-doc');
INSERT INTO FundingTypes (ID,ftname,doc_type,detail) VALUES (3,'Original Research',9,'all p-doc');
INSERT INTO FundingTypes (ID,ftname,doc_type,detail) VALUES (4,'Facility/Center-Oriented',8,'research centers, user facilities, and collaborative networks (f-doc)');
INSERT INTO FundingTypes (ID,ftname,doc_type,detail) VALUES (5,'R&D Equipment',10,'q-doc');
INSERT INTO FundingTypes (ID,ftname,doc_type,detail) VALUES (6,'R&D Technology',10,'q-doc');
INSERT INTO FundingTypes (ID,ftname,doc_type,detail) VALUES (7,'Exchange/Visitor',11,'s-doc');
INSERT INTO FundingTypes (ID,ftname,doc_type,detail) VALUES (8,'Meetings/Workshops',11,'conferences, meetings, workshops, schools, s-doc');
INSERT INTO FundingTypes (ID,ftname,doc_type,detail) VALUES (9,'Service-Oriented',11,'any other service programs, s-doc');
INSERT INTO FundingTypes (ID,ftname,doc_type,detail) VALUES (253,'Other Funding Opportunities',252,'zw-doc');
INSERT INTO FundingTypes (ID,ftname,doc_type,detail) VALUES (254,'Related to multiple CORE docs',255,'two or more CORE docs');
INSERT INTO FundingTypes (ID,ftname,doc_type,detail) VALUES (255,'Unrelated to CORE Proposals',255,'not-related to any CORE docs');

CREATE TABLE FundingPrograms (
    fID INT UNSIGNED PRIMARY KEY AUTO_INCREMENT,    -- funding program ID
    fname VARCHAR(255) COLLATE utf8mb4_general_ci NOT NULL,
    ftype TINYINT UNSIGNED NOT NULL,      -- e.g., 1=p-doc/mainstream, 2=p-doc/highrisk, 3=p-doc, 4=facility/center-oriented(f-doc), 5-6=q-doc, 7-9=s-doc, 254=other, 255=unrelated
    email VARCHAR(254) COLLATE utf8mb4_general_ci,
    contact VARCHAR(255) COLLATE utf8mb4_general_ci,
    homepage VARCHAR(255) COLLATE utf8mb4_general_ci,
    sponsor_ID INT UNSIGNED NOT NULL,
    freq TINYINT UNSIGNED default 0,   -- 0=one time, 1=halfyear=biannually, 2=annually, 4=biennially, 10=5yrs, freq/2=year# for the funding call period, 255=irregular
    deadline DATETIME,      -- null means no application deadline or rolling
    start_time DATETIME,    -- the earliest possible funding start time
    duration_min TINYINT UNSIGNED,               -- support duration in number of years
    duration_max TINYINT UNSIGNED,               -- support duration in number of years
    budget_min DOUBLE,           -- mininim budget for each project
    budget_ave DOUBLE,           -- average budget for each project
    budget_max DOUBLE,           -- maximum budget for each project
    budget_tot DOUBLE,           -- total budget for all projects
    currency VARCHAR(60) COLLATE utf8mb4_general_ci NOT NULL default 'US Dollars',
    totnum INT UNSIGNED,        -- total number of projects to be supported
    sponsored JSON,        -- JSON array (aID,funding,starttime,duration)
    is_active TINYINT(1) NOT NULL default 1,  -- if the program is active or not
    detail TEXT COLLATE utf8mb4_general_ci,
    -- ... funding criteria, eligibility requirements, etc.
    FOREIGN KEY (sponsor_ID) REFERENCES MemberOrganizations(oID),
    FOREIGN KEY (ftype) REFERENCES FundingTypes(ID),
    INDEX (sponsor_ID,ftype,fname),
    INDEX (ftype,deadline,is_active)
);

CREATE TABLE FundingManagers (  -- managers of each funding program
    fID INT UNSIGNED NOT NULL,      -- funding program ID
    mID BIGINT UNSIGNED NOT NULL,   -- member ID of the manager
    is_manager TINYINT(1) NOT NULL default 1,  -- 0=stuff view only
    FOREIGN KEY (fID) REFERENCES FundingPrograms (fID),
    FOREIGN KEY (mID) REFERENCES Members (mID),
    PRIMARY KEY (fID, mID)
);

CREATE TABLE FundingBranches (  -- what research branches the funding program aims at
    fID INT UNSIGNED NOT NULL,
    bID INT UNSIGNED NOT NULL,
    FOREIGN KEY (fID) REFERENCES FundingPrograms(fID),
    FOREIGN KEY (bID) REFERENCES ResearchBranches(bID),
    PRIMARY KEY (fID,bID)
);

CREATE TABLE SourceTypes (  -- what type of funding sources a proposal desires
    ID TINYINT UNSIGNED PRIMARY KEY,
    stname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
    is_active TINYINT(1) NOT NULL default 1,   -- 0 means a decommissioned type
    detail TEXT COLLATE utf8mb4_general_ci,
    INDEX (stname,is_active)
);
INSERT INTO SourceTypes (ID,stname,detail) VALUES (1,'Single Agency','a single agency for full funding is desired');
INSERT INTO SourceTypes (ID,stname,detail) VALUES (2,'Multiple Agencies','two or more agencies can cofund');
INSERT INTO SourceTypes (ID,stname,detail) VALUES (3,'CrowdFunding','crowdfunding only');
INSERT INTO SourceTypes (ID,stname,detail) VALUES (255,'Any Funding Sources','can be funded from any sources');

CREATE TABLE FundingProposals (  -- extra information for f/p/q/s-doc (to be left joined)
    dID BIGINT UNSIGNED PRIMARY KEY, -- Link to the main proposal Document (e.g., p-docs)
    start_time DATETIME,             -- desired/realistic start time
    duration_min TINYINT UNSIGNED NOT NULL,   -- minimum project duration in number of years
    duration_max TINYINT UNSIGNED,      -- maximum duration in number of years, NULL means a fixed duration
    extension TINYINT UNSIGNED,   -- additional number of years for no-cost extension
    budget_min DOUBLE NOT NULL,   -- minimum total budget
    budget_max DOUBLE,                   -- maximum total budget, NULL means a fixed budget
    currency VARCHAR(60) COLLATE utf8mb4_general_ci NOT NULL default 'US Dollars',
    is_nonprofit TINYINT(1) NOT NULL,    -- true if the proposal is from 501(c)(3) for tax purposes, false otherwise
    sourcetype TINYINT UNSIGNED NOT NULL default 1,   -- 1=single agency, 2=multiple agencies allowed, 3=crowdfunding only, 255=all types of funding/donations are allowed
    refund TINYINT(1) NOT NULL default 0,  -- refund=1 to refund if not receive full funding
    funding_committed DOUBLE,
    funding_received DOUBLE,
    deadline DATETIME,      -- deadline for committing the support; NULL means rolling no deadline
    pstatus VARCHAR(60) COLLATE utf8mb4_general_ci DEFAULT 'submitted',  -- proposal status, e.g., 'submitted', 'abandoned/inactive', 'partially/fully funded', 'rejected by all'
    sponsors JSON,    -- JSON array of [sponsor_name,sponsor_type,sponsor_id,amount] committed support from persons or agencies
    is_active TINYINT(1) NOT NULL default 1,  -- if the project is still active, inactive due to reasons other than finished
    finished TINYINT(1) NOT NULL default 0,   -- if the project is finished
    homepage VARCHAR(255) COLLATE utf8mb4_general_ci,
    progress JSON,    -- JSON array of [datetime, progress/milestone] update of the project
    results TEXT COLLATE utf8mb4_general_ci,  -- summary of final products/results of the project
    N1 INT UNSIGNED NOT NULL default 0,  -- number of pscore1
    N2 INT UNSIGNED NOT NULL default 0,  -- number of pscore2
    N3 INT UNSIGNED NOT NULL default 0,  -- number of pscore3
    S1 DOUBLE NOT NULL default 0,        -- average partial score1 = soundness (mainly for high-risk proposals)
    S2 DOUBLE NOT NULL default 0,        -- average partial score2 = impact
    S3 DOUBLE NOT NULL default 0,        -- average partial score3 = feasibility
    -- see the main table Documents for S_ave: Overall score for main-stream proposals
    -- ... details specific to proposal type (budget breakdown, project duration etc.)
    FOREIGN KEY (dID) REFERENCES Documents(dID),
    FOREIGN KEY (sourcetype) REFERENCES SourceTypes(ID),
    INDEX (deadline,sourcetype,is_active)
);

CREATE TABLE SeekingList ( -- list of funding programs that a proposal is seekimg to apply to
    dID BIGINT UNSIGNED NOT NULL,
    fID INT UNSIGNED NOT NULL,
    ts DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (fID) REFERENCES FundingPrograms(fID),
    FOREIGN KEY (dID) REFERENCES FundingProposals(dID),
    PRIMARY KEY (dID,fID)
);
-- an entry will be removed if a decision is made; all other seeking programs from a proposal will be deleted if fully funded;

CREATE TABLE RejectedList ( -- list of funding programs that rejected a proposal
    dID BIGINT UNSIGNED NOT NULL,
    fID INT UNSIGNED NOT NULL,
    ts DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (fID) REFERENCES FundingPrograms(fID),
    FOREIGN KEY (dID) REFERENCES FundingProposals(dID),
    PRIMARY KEY (dID,fID)
);

CREATE TABLE FundedList ( -- list funded proposals with funding programs
    fID INT UNSIGNED NOT NULL,
    dID BIGINT UNSIGNED NOT NULL,
    committed DOUBLE NOT NULL,
    ts DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (fID) REFERENCES FundingPrograms(fID),
    FOREIGN KEY (dID) REFERENCES FundingProposals(dID),
    PRIMARY KEY (fID,dID)
);


-- Job/Hiring-related tables

CREATE TABLE JobPostings (
    jID BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    oID INT UNSIGNED NOT NULL,       -- the hiring organization
    department VARCHAR(255) COLLATE utf8mb4_general_ci,
    title VARCHAR(255) COLLATE utf8mb4_general_ci NOT NULL,
    jtype VARCHAR(60) COLLATE utf8mb4_general_ci,      -- e.g., 'faculty', 'postdoc', 'scientist'
    email VARCHAR(254) COLLATE utf8mb4_general_ci,
    contact VARCHAR(255) COLLATE utf8mb4_general_ci,
    homepage VARCHAR(255) COLLATE utf8mb4_general_ci,
    role_min SMALLINT UNSIGNED,   -- requirement on member role level 'mrole' (e.g., revewership+)
    AL_min VARCHAR(5) COLLATE ascii_general_ci,  -- e.g., > L2.5/3
    ALS_min DOUBLE,               -- e.g., >100
    ECP_min DOUBLE,               -- e.g., ECP > 1000 in total, >500 in Phys.NP, moderatorship+
    requirement TEXT COLLATE utf8mb4_general_ci,  -- more detailed AL/ECP requirements, forming a mysql query string (e.g., minimum ALS and ECP in multiple branches and/or doc-types)
    is_active TINYINT(1) NOT NULL default 1,  -- if it is still actively hiring
    deadline DATETIME NOT NULL,
    detail TEXT COLLATE utf8mb4_general_ci,
    -- ... required skills, experience, salary range etc.
    FOREIGN KEY (oID) REFERENCES MemberOrganizations(oID),
    INDEX (oID,deadline),
    INDEX (jtype,title,deadline)
);

CREATE TABLE JobManagers (  -- managers of each job hiring
    jID BIGINT UNSIGNED NOT NULL,   -- job posting ID
    mID BIGINT UNSIGNED NOT NULL,   -- member ID of the manager
    is_manager TINYINT(1) NOT NULL default 1,  -- 0=stuff view only
    FOREIGN KEY (jID) REFERENCES JobPostings (jID),
    FOREIGN KEY (mID) REFERENCES Members (mID),
    PRIMARY KEY (jID, mID)
);

CREATE TABLE JobBranches (
    jID BIGINT UNSIGNED NOT NULL,
    bID INT UNSIGNED NOT NULL,   -- branch ID
    FOREIGN KEY (jID) REFERENCES JobPostings(jID),
    FOREIGN KEY (bID) REFERENCES ResearchBranches(bID),
    PRIMARY KEY (jID, bID)
);

CREATE TABLE JobCandidates (
    jID BIGINT UNSIGNED NOT NULL,
    candidate BIGINT UNSIGNED NOT NULL,    -- candidates that meet the requirements and have applied
    offered TINYINT(1),      -- NULL=undecided, 0=rejected, 1=offered by jID
    accepted TINYINT(1),     -- NULL=undecided, 0=declined, 1=accepted by candidate
    CONSTRAINT CHK_commit CHECK ((offered IS NOT NULL AND offered = 1) OR (accepted IS NULL)),  -- candidate can only respond if offered
    FOREIGN KEY (jID) REFERENCES JobPostings(jID),
    FOREIGN KEY (candidate) REFERENCES Members(mID),
    PRIMARY KEY (jID, candidate)
);


-- Update-related tables

CREATE TABLE UpdateCommentList (   -- list of creditable comment/review changes (new rating and >=5) that need to be updated at more idle times
    cID BIGINT UNSIGNED UNIQUE NOT NULL,
    FOREIGN KEY (cID) REFERENCES CRComments(cID)
);
-- before update, remove rows whose parent dID is in UpdateDocList
-- e.g.,  INSERT INTO UpdateCommentList (cID) VALUES (nnn), (nnn2), (nnn3) ON DUPLICATE KEY UPDATE cID=cID;

CREATE TABLE UpdateDocList (   -- list of doc changes (new rating & >=5 or new phase) that need to be updated at more idle times
    dID BIGINT UNSIGNED UNIQUE NOT NULL,
    FOREIGN KEY (dID) REFERENCES FundingProposals(dID)
);
-- after each update, remove the corresponding dID row, and add mID's to UpdateMember[AL/ECP]List

CREATE TABLE UpdateMemberALList (   -- list of member IDs for updating their AL at more idle times
    mID BIGINT UNSIGNED UNIQUE NOT NULL,
    FOREIGN KEY (mID) REFERENCES Members(mID)
);

CREATE TABLE UpdateMemberECPList (   -- list of member IDs for updating their ECP at more idle times
    mID BIGINT UNSIGNED UNIQUE NOT NULL,
    FOREIGN KEY (mID) REFERENCES Members(mID)
);

CREATE TABLE UpdateParaTypes (   -- types of parameter changes that need to be updated
    ID TINYINT UNSIGNED PRIMARY KEY,
    utname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
    is_active TINYINT(1) NOT NULL default 1,   -- 0 means a decommissioned type
    detail TEXT COLLATE utf8mb4_general_ci,
    INDEX (utname,is_active)
);
INSERT INTO UpdateParaTypes (ID,utname,detail) VALUES (1,'all','update all AL and ECP calculations');
INSERT INTO UpdateParaTypes (ID,utname,detail) VALUES (2,'AL','update all AL calculations');
INSERT INTO UpdateParaTypes (ID,utname,detail) VALUES (3,'AL_a-doc','update AL for a-docs');
INSERT INTO UpdateParaTypes (ID,utname,detail) VALUES (4,'AL_one branch','update one branch AL');
INSERT INTO UpdateParaTypes (ID,utname,detail) VALUES (10,'ECP','update all ECP calculations');
INSERT INTO UpdateParaTypes (ID,utname,detail) VALUES (11,'ECP_docs','update ECP of all docs');
INSERT INTO UpdateParaTypes (ID,utname,detail) VALUES (12,'ECP_invited','update ECP of invited reviews');
INSERT INTO UpdateParaTypes (ID,utname,detail) VALUES (13,'ECP_contributed','update ECP of contributed reviews');
INSERT INTO UpdateParaTypes (ID,utname,detail) VALUES (14,'ECP_comments','update ECP of creditable comments');
INSERT INTO UpdateParaTypes (ID,utname,detail) VALUES (15,'ECP_ratings','update all rating ECP calculations');
INSERT INTO UpdateParaTypes (ID,utname,detail) VALUES (16,'ECP_one branch','update one branch ECP');
INSERT INTO UpdateParaTypes (ID,utname,detail) VALUES (17,'ECP_one doc-type','update one doc-type ECP');
INSERT INTO UpdateParaTypes (ID,utname,detail) VALUES (18,'ECP_branch & doc-type','update one doc-type ECP in one branch');
-- many other related to different branches and doc-types

CREATE TABLE UpdateParameterList (   -- list of parameter changes for updating all relevant AL/ECP at more idle times
    ID TINYINT UNSIGNED NOT NULL,  -- parameter type for the same update steps
    bID INT UNSIGNED NOT NULL default 1,   -- branch ID, 1=all
    doctype TINYINT UNSIGNED NOT NULL default 255,  -- doc-type ID, 255=all
    FOREIGN KEY (ID) REFERENCES UpdateParaTypes(ID),
    FOREIGN KEY (bID) REFERENCES ResearchBranches(bID),
    FOREIGN KEY (doctype) REFERENCES DocTypes(ID),
    PRIMARY KEY (ID, bID, doctype)
);


-- For temperatory document drafts

CREATE TABLE DocDrafts (  -- document drafts that can also be used for future development on on-site collaborative research and related activities
    dID BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,    -- unique identifier of a parent document draft
    dtype TINYINT UNSIGNED NOT NULL default 1,         -- e.g., 'o-docs' (default), 'r-docs', 'p-docs'
    title TEXT COLLATE utf8mb4_general_ci NOT NULL,
    abstract TEXT COLLATE utf8mb4_general_ci NOT NULL,
    notes VARCHAR(255) COLLATE utf8mb4_general_ci,     -- page#, fig#, other brief comments or changes for new revision
    submitter_ID BIGINT UNSIGNED NOT NULL,             -- member ID of the submitter
    author_list JSON,        -- JSON array of authors without affiliations: [[author_name=pub_name, mID , significance_factor=duty], ...] (author list in order, editors for c/m/w docs, organizers for f/s/t docs), null means submitter is the sole author  -- more complex: {"authors" : [[pub_name, mID, duty, [list of order numbers of affiliations]], ...], "affiliations": [[order#, name], ...]}
    link_list JSON,          -- JSON array of external links [[sID,esname,link],...]
    branch_list JSON,        -- JSON array of research branches [{"bID":int,"num":int,"impact":int},...] up to 3 branches
    tID INT UNSIGNED,        -- topic ID assigned if desired
    main_size INT UNSIGNED,  -- size in bytes for the main pdf file (pdf only)
    suppl_size INT UNSIGNED,  -- size in bytes for the suppl file (pdf or zip)
    suppl_ext VARCHAR(7) COLLATE ascii_general_ci,  -- suppl file type: NULL if no suppl file, currently (pdf, zip) supported (possible future extensions: gz,7z,rar,tgz,tar.gz,tar.bz2)
    main_pages SMALLINT UNSIGNED,  -- number of pages in main file
    main_figs SMALLINT UNSIGNED,  -- number of figures in main file
    main_tabs SMALLINT UNSIGNED,  -- number of tables in main file
    datetime_added DATETIME DEFAULT CURRENT_TIMESTAMP,   -- datetime added to database
    pub_date DATE,  -- YYYY-MM-DD, date published of an old doc
    recv_date DATE, -- YYYY-MM-DD, date received of an old doc
    last_update_time DATETIME DEFAULT (datetime_added) ON UPDATE CURRENT_TIMESTAMP,
    full_text LONGTEXT COLLATE utf8mb4_general_ci,       -- Storing text content for search or retrieval (optional)
    -- ... more metadata
    FOREIGN KEY (dtype) REFERENCES DocTypes(ID),
    FOREIGN KEY (submitter_ID) REFERENCES Members(mID),
    FOREIGN KEY (tID) REFERENCES ResearchTopics(tID),
    INDEX (submitter_ID,dtype)
--    FULLTEXT INDEX (title, abstract)          -- Fulltext index for title/abstract search
);

CREATE TABLE DocDraftAuthors (   -- co-authors who can edit/view/approve DocDrafts; but only submitter can submit documents; submitter should not be included here even if is a co-author.
    dID BIGINT UNSIGNED NOT NULL,
    mID BIGINT UNSIGNED NOT NULL,
    is_editor TINYINT(1) NOT NULL default 1,  -- 0 means viewer only
    is_approved TINYINT(1) NOT NULL default 0,  -- 1 means the author approved it
    is_locked TINYINT(1) NOT NULL default 0,  -- 1 means the author's choice is locked no matter any future changes (completely trust the submitter if it is approval; completely refute the doc if it is disapproval); 0 means any future update needs the author's re-approval.
    FOREIGN KEY (dID) REFERENCES DocDrafts(dID) ON DELETE CASCADE,
    FOREIGN KEY (mID) REFERENCES Members(mID),
    PRIMARY KEY (dID, mID),         -- Ensure no duplicate authors for the same document
    INDEX (dID,is_approved)
); -- can not submit until all approvals received


-- CORE system tables

CREATE TABLE NewsTypes (   -- types of news/announcement items from CORE
    ID TINYINT UNSIGNED PRIMARY KEY,
    ntname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
    is_active TINYINT(1) NOT NULL default 1,  -- 0 means a decommissioned type
    detail TEXT COLLATE utf8mb4_general_ci,
    INDEX (ntname,is_active)
);
INSERT INTO NewsTypes (ID,ntname,detail) VALUES (1,'System','System announcements');
INSERT INTO NewsTypes (ID,ntname,detail) VALUES (2,'News','News announcements');
INSERT INTO NewsTypes (ID,ntname,detail) VALUES (255,'Misc','miscellaneous information');

CREATE TABLE News (   -- announcements from CORE
    nID INT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    ntype TINYINT UNSIGNED NOT NULL default 1,   -- 255=misc, 1=system, 2=news
    mID BIGINT UNSIGNED NOT NULL,                -- mID who last updated the item
    title VARCHAR(255) COLLATE utf8mb4_general_ci NOT NULL,
    vis TINYINT UNSIGNED NOT NULL default 0,  -- 0=default visible by all, 1=expired; 10-59 visible if mrole>=vis; 110-159(160=admin) visible if admin_role/10+100>vis; 255=draft
    expire_time DATETIME NOT NULL default (NOW() + INTERVAL 1 DAY),  -- it will be expired in one day by default
    datetime_added DATETIME DEFAULT CURRENT_TIMESTAMP,   -- datetime added to database
    last_update_time DATETIME DEFAULT (datetime_added) ON UPDATE CURRENT_TIMESTAMP,
    detail TEXT COLLATE utf8mb4_general_ci,   -- full announcement/information
    FOREIGN KEY (mID) REFERENCES Members(mID), -- who post the announcement
    FOREIGN KEY (ntype) REFERENCES NewsTypes (ID),
    INDEX (mID, ntype, vis),
    FULLTEXT INDEX (title,detail)
);


-- Cron / System Tasks Table

CREATE TABLE SystemTasks (
    task_id SMALLINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    task_name VARCHAR(50) COLLATE utf8mb4_general_ci NOT NULL UNIQUE,  -- e.g., 'calculate_ecp', 'send_emails', 'announce_doc', 'announce_com'
    freq_minutes SMALLINT UNSIGNED NOT NULL,         -- e.g., 60 (run every hour)
    last_run DATETIME DEFAULT CURRENT_TIMESTAMP,
    is_running TINYINT(1) DEFAULT 0,   -- lock to prevent overlapping runs
    last_error DATETIME,  -- last time when task failed
    INDEX (last_run, is_running)
);
INSERT INTO SystemTasks (task_name, freq_minutes) VALUES 
('announce_comment', 10), -- every 10 min
('announce_doc', 1440),   -- every 24 hrs
('calc_ecp', 1440),   -- Run daily
('calc_als', 10080),  -- Run weekly
('move_logs',20160),  -- Run bi-weekly
('cleanup_ratelimits', 1440),   -- Run daily
('check_misc', 60);   -- Run hourly

CREATE TABLE `RateLimits` (
    `action_key` VARCHAR(128) COLLATE utf8mb4_general_ci NOT NULL,
    `attempts` TINYINT UNSIGNED NOT NULL DEFAULT 1,
    `first_attempt_time` INT UNSIGNED NOT NULL,
    PRIMARY KEY (`action_key`)
);


-- -------------------------------------------------------------------------------------------
-- below for future development: collaboration features (that could be charged to member institutions or depend on the tier of the institution membership), similar to overleaf, google doc, etc. for doc-drafts
-- -------------------------------------------------------------------------------------------

CREATE TABLE CollTypes (   -- collaboration types or sizes
    ID TINYINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    ctname VARCHAR(60) COLLATE ascii_general_ci UNIQUE NOT NULL,
    size INT UNSIGNED NOT NULL,  -- max number of members allowed
    is_active TINYINT(1) NOT NULL default 1,  -- 0 means a decommissioned type
    detail TEXT COLLATE utf8mb4_general_ci,
    INDEX (ctname,size,is_active)
);
INSERT INTO CollTypes (ID,ctname,size,detail) VALUES (1,'Free',10000,'free for publication association only, no paid features');
INSERT INTO CollTypes (ID,ctname,size,detail) VALUES (2,'Trio',3,'up to 3 members');
INSERT INTO CollTypes (ID,ctname,size,detail) VALUES (3,'Mini',10,'up to 10 members');
INSERT INTO CollTypes (ID,ctname,size,detail) VALUES (4,'Small',30,'up to 30 members');
INSERT INTO CollTypes (ID,ctname,size,detail) VALUES (5,'Modest',100,'up to 100 members');
INSERT INTO CollTypes (ID,ctname,size,detail) VALUES (6,'Medium',300,'up to 300 members');
INSERT INTO CollTypes (ID,ctname,size,detail) VALUES (7,'Large',1000,'up to 1000 members');
INSERT INTO CollTypes (ID,ctname,size,detail) VALUES (8,'Huge',3000,'up to 3000 members');
INSERT INTO CollTypes (ID,ctname,size,detail) VALUES (9,'Gigantic',10000,'up to 10000 members');
INSERT INTO CollTypes (ID,ctname,size,detail) VALUES (255,'Unlimited',4294967295,'no limit on member numbers');
-- higher-tier sponsoring institutions can host larger collaborations (no extra fees)
-- tier one is free or for old collaborations for publication association only, no other paid features
-- tier two fo Trio may be free as well for more features; higher tiers require annual subscription fees

CREATE TABLE Collaborations (   -- CORE collaborations
    ID BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    colltype TINYINT UNSIGNED NOT NULL, 
    parent_ID BIGINT UNSIGNED,         -- NULL if it is NOT a sub-collaboration
    collname VARCHAR(255) COLLATE utf8mb4_general_ci NOT NULL,  -- full name
    abbr VARCHAR(30) COLLATE ascii_general_ci UNIQUE NOT NULL,  -- short ACRONYM 
    bID INT UNSIGNED NOT NULL,         -- branch ID
    creator BIGINT UNSIGNED NOT NULL,  -- collaboration creator or lead PI
    is_active TINYINT(1) NOT NULL default 1,
    email VARCHAR(254) COLLATE utf8mb4_general_ci,
    contact VARCHAR(255) COLLATE utf8mb4_general_ci,
    start_date DATE DEFAULT CURRENT_DATE,
    task TEXT COLLATE utf8mb4_general_ci,
    UNIQUE (collname, bID),
    FOREIGN KEY (colltype) REFERENCES CollTypes (ID),
    FOREIGN KEY (creator) REFERENCES Members(mID),
    FOREIGN KEY (bID) REFERENCES ResearchBranches(bID),
    FOREIGN KEY (parent_ID) REFERENCES Collaborations(ID), -- Self-referencing for hierarchy
    INDEX (parent_ID,abbr,colltype,is_active),
    INDEX (creator,bID,abbr,colltype,is_active)
);

CREATE TABLE CollaborationMembers (    -- members listed in each collaboration
    coll_ID BIGINT UNSIGNED NOT NULL,
    mID BIGINT UNSIGNED NOT NULL,
    num TINYINT UNSIGNED NOT NULL default 1,  -- stint number
    crole TINYINT UNSIGNED default 1,  -- 0=non-academic stuff(e.g., administrative, no AL/ECP), 1=general member(5%), 2=important role(7.5%), 3=leading/major role(10% if not explicitly in the author list)
    position VARCHAR(60) COLLATE utf8mb4_general_ci,    -- 'PI', 'senior', 'member', 'student'
    admin_role SMALLINT UNSIGNED NOT NULL default 300,  -- 100=viewer, 300=editor (edit doc drafts and add references), 600=admin
    start_date DATE DEFAULT CURRENT_DATE,
    end_date DATE,    -- NULL means permanent or indefinite
    FOREIGN KEY (coll_ID) REFERENCES Collaborations(ID),
    FOREIGN KEY (mID) REFERENCES Members(mID),
    FOREIGN KEY (admin_role) REFERENCES AdminTypes (ID),
    PRIMARY KEY (coll_ID, mID, num)
);
-- admin(>=600) should have full control (add/remove members), editor can edit drafts and reference list
-- when num++, make sure that in the earlier stint, end_date was set otherwise will be set to current start_date-1.
-- for each publication, explicit list of authors still take up to 875%. Everyone else in the collaboration will take 10% if crole=3, 7.5% if crole=2, 5% if crole=1. 

CREATE TABLE CollaborationDocs (   -- published documents by each collaboration
    coll_ID BIGINT UNSIGNED NOT NULL,
    dID BIGINT UNSIGNED NOT NULL,
    start_date DATE NOT NULL,       -- must provide the date range to determine authors
    end_date DATE NOT NULL DEFAULT CURRENT_DATE,   -- default is the time at submission
    FOREIGN KEY (coll_ID) REFERENCES Collaborations(ID),
    FOREIGN KEY (dID) REFERENCES Documents(dID),
    PRIMARY KEY (coll_ID, dID),
    INDEX (coll_ID, start_date, end_date)
);  -- multiple collaborations can publish together
-- For ECP/AL, only count members who is active during the project period (between start_date and end_date)

CREATE TABLE CollaborationDrafts (  -- doc drafts in a collaboration to be published
    coll_ID BIGINT UNSIGNED NOT NULL,
    dID BIGINT UNSIGNED NOT NULL,
    start_date DATE NOT NULL,
    end_date DATE,             -- not needed until submission
    FOREIGN KEY (coll_ID) REFERENCES Collaborations(ID),
    FOREIGN KEY (dID) REFERENCES DocDrafts(dID) ON DELETE CASCADE,
    PRIMARY KEY (coll_ID, dID)
);  -- multiple collaborations can work on the same draft

CREATE TABLE CollaborationRefs (   -- list of references kept by each collaboration
    coll_ID BIGINT UNSIGNED NOT NULL,
    dID BIGINT UNSIGNED NOT NULL,
    FOREIGN KEY (coll_ID) REFERENCES Collaborations(ID),
    FOREIGN KEY (dID) REFERENCES Documents(dID),
    PRIMARY KEY (coll_ID, dID)
);


-- -------------------------------------------------------------------------------------------
-- ------------------- Triggers, Functions, Procedures that may not be allowed ---------------
-- -------------------------------------------------------------------------------------------

DELIMITER // -- Temporarily change delimiter to allow ';' inside the procedure body
CREATE TRIGGER trg_crcomments
BEFORE INSERT ON CRComments
FOR EACH ROW
BEGIN
      DECLARE nid, nnth INT UNSIGNED;
      DECLARE dID1 BIGINT UNSIGNED;
      DECLARE ctype1 TINYINT UNSIGNED;
      DECLARE ts0, ts1 DATETIME;
      SELECT dID, ctype, ts INTO dID1, ctype1, ts1 FROM Comments WHERE cID = NEW.cID;
      SELECT reset_time INTO ts0 FROM Documents WHERE dID = dID1;
      UPDATE Documents SET Ntot_invited = Ntot_invited + (ctype1=1), Ntot_contributed = Ntot_contributed + (ctype1=2), Ntot_comments = Ntot_comments + (ctype1=3) WHERE dID = dID1;
      SELECT COALESCE(MAX(t2.ID_num), 0) + 1 INTO nid FROM Comments AS t1 INNER JOIN CRComments AS t2 ON t1.cID = t2.cID WHERE t1.dID = dID1;
      SELECT COALESCE(MAX(t2.Nth), 0) + 1 INTO nnth FROM Comments AS t1 INNER JOIN CRComments AS t2 ON t1.cID = t2.cID WHERE t1.dID = dID1 AND t1.ctype = ctype1 AND t1.ts >= ts0;
      SET NEW.ID_num = nid;
      SET NEW.ID_alphanum = CONV(nid,10,36);
      SET NEW.Nth = nnth;
      SET NEW.T = TIMESTAMPDIFF(MINUTE, ts0, ts1);
END//
DELIMITER ; -- Revert delimiter back to ';'

DELIMITER // -- Temporarily change delimiter to allow ';' inside the procedure body
CREATE TRIGGER trg_docratings
AFTER INSERT ON DocRatings
FOR EACH ROW
BEGIN
      DECLARE nrate INT UNSIGNED;
      UPDATE Documents SET S_ave = (S_ave * Ntot_ratings + New.score) / (Ntot_ratings + 1), nrate = Ntot_ratings, Ntot_ratings = (Ntot_ratings + 1)  WHERE dID = NEW.dID;
      UPDATE FundingProposals SET S1 = IFNULL((New.pscore1 + S1 * N1) / (N1 + 1), S1), N1 = N1 + (New.pscore1 IS NOT NULL), S2 = IFNULL((New.pscore2 + S2 * N2) / (N2 + 1), S2), N2 = N2 + (New.pscore2 IS NOT NULL), S3 = IFNULL((New.pscore3 + S3 * N3) / (N3 + 1), S3), N3 = N3 + (New.pscore3 IS NOT NULL) WHERE dID = NEW.dID;
      IF nrate>=5 THEN INSERT INTO UpdateDocList (dID) VALUES (NEW.dID) ON DUPLICATE KEY UPDATE dID=dID;
      END IF;
END//

CREATE TRIGGER trg1_docratings
BEFORE INSERT ON DocRatings
FOR EACH ROW
BEGIN
      DECLARE nnth INT UNSIGNED;
      DECLARE dID1 BIGINT UNSIGNED;
      DECLARE ts0 DATETIME;
      SELECT reset_time, Ntot_ratings INTO ts0, nnth FROM Documents WHERE dID = dID1;
      SET NEW.Nth = nnth+1;
      SET NEW.ts = NOW();
      SET NEW.T = TIMESTAMPDIFF(MINUTE, ts0, NEW.ts);
END//
DELIMITER ; -- Revert delimiter back to ';'

DELIMITER // -- Temporarily change delimiter to allow ';' inside the procedure body
CREATE TRIGGER trg_commentratings
AFTER INSERT ON CommentRatings
FOR EACH ROW
BEGIN
      DECLARE nrate INT UNSIGNED;
      UPDATE CRComments SET S_ave = (S_ave * N_ratings + New.score) / (N_ratings + 1), nrate = Ntot_ratings, N_ratings = (N_ratings + 1)  WHERE cID = NEW.cID;
      IF nrate>=5 THEN INSERT INTO UpdateCommentList (cID) VALUES (NEW.cID) ON DUPLICATE KEY UPDATE cID=cID;
      END IF;
END//

CREATE TRIGGER trg1_commentratings
BEFORE INSERT ON CommentRatings
FOR EACH ROW
BEGIN
      DECLARE nnth INT UNSIGNED;
      DECLARE dID1 BIGINT UNSIGNED;
      DECLARE ts0 DATETIME;
      SELECT dID INTO dID1 FROM Comments WHERE cID = NEW.cID;
      SELECT N_ratings INTO nnth FROM CRComments WHERE cID = NEW.cID;
      SELECT reset_time INTO ts0 FROM Documents WHERE dID = dID1;
      SET NEW.Nth = nnth+1;
      SET NEW.ts = NOW();
      SET NEW.T = TIMESTAMPDIFF(MINUTE, ts0, NEW.ts);
END//
DELIMITER ; -- Revert delimiter back to ';'


-- stored procedures and functions

DELIMITER // -- Temporarily change delimiter to allow ';' inside the procedure body
CREATE FUNCTION AL_alloc (s DOUBLE, duty TINYINT UNSIGNED)  -- allocation of AL value according to author's contribution/duty
RETURNS DOUBLE DETERMINISTIC
BEGIN
    DECLARE p,c1,c2,c3,c4,lnp,del,cap DOUBLE;
    IF duty = 100 THEN RETURN s;
    ELSEIF duty < 10 THEN RETURN 0e0;
    ELSE
      SET p = CAST(duty AS DOUBLE)/100e0;
      SET lnp = LN(p);
      SET c1 = lnp/(p-1e0)-0.5e0;
      SET c2 = c1 + 1e0;
      SET del = LEAST(0.5e0, lnp*(1e0-1e0/LN(2)));
      SET cap = 5e0 + lnp/ LN(2);
      SET c3 = cap - del - lnp;
      SET c4 = c3 + 2e0 * del;
      IF s<c1 THEN RETURN p*s;
      ELSEIF s<c2 THEN RETURN p*lnp/(p-1e0)-0.5e0*p*POW(c2-s,2)+0.5e0*POW(s-c1,2);
      ELSEIF s<c3 THEN RETURN s+lnp;
      ELSEIF s<c4 THEN RETURN cap - POW(c4-s,2)/4e0/del;
      ELSE RETURN cap;
      END IF;
    END IF;
END//
DELIMITER ; -- Revert delimiter back to ';'

CREATE FUNCTION Fac_fo (ano TINYINT UNSIGNED)   -- openness factor
RETURNS DOUBLE DETERMINISTIC
RETURN CASE ano WHEN 1 THEN 1e0 WHEN 2 THEN 0.5e0 WHEN 3 THEN 0.5e0 WHEN 4 THEN 0.25e0 WHEN 5 THEN 0.25e0 ELSE 0e0 END;

CREATE FUNCTION Fac_fa (s DOUBLE)  -- attention factor
RETURNS DOUBLE DETERMINISTIC
RETURN IF(s<0e0, POW(2e0,-3e0*s-2), POW(2e0,s-2e0));

CREATE FUNCTION Fac_febr (nthr INT UNSIGNED, n INT UNSIGNED)  -- rating
RETURNS DOUBLE DETERMINISTIC
RETURN IF(n<=nthr, 1e0, 0.5e0*SQRT(CAST(nthr AS DOUBLE)/CAST(n AS DOUBLE)));

CREATE FUNCTION Fac_febc (nthr INT UNSIGNED, n INT UNSIGNED)  -- comment/review
RETURNS DOUBLE DETERMINISTIC
RETURN IF(n<=nthr/2, 1e0, IF(n<=nthr, EXP(-POW(CAST(n AS DOUBLE)/CAST(nthr AS DOUBLE)-0.5e0,2)), EXP(-0.25e0)*CAST(nthr AS DOUBLE)/CAST(n AS DOUBLE)));

CREATE FUNCTION Fac_ft (tresp SMALLINT UNSIGNED, t INT UNSIGNED)
RETURNS DOUBLE DETERMINISTIC
RETURN IF(t <= tresp*1440, 1e0, IF(t <= tresp*2880, EXP(-0.125e0*POW(CAST(t AS DOUBLE)/CAST(tresp AS DOUBLE)/1440e0-1e0,2)), EXP(-0.125)*SQRT(2880e0*CAST(tresp AS DOUBLE)/CAST(t AS DOUBLE))));

DELIMITER // -- Temporarily change delimiter to allow ';' inside the procedure body
CREATE FUNCTION Fac_fd (rtype TINYINT UNSIGNED, s DOUBLE) -- rating
RETURNS DOUBLE DETERMINISTIC
BEGIN
  IF rtype=1 THEN RETURN 0.4e0*POW(2e0,s-2e0);   -- with invited review
  ELSEIF rtype=2 THEN RETURN 0.2e0*POW(2e0,s-2e0);  -- with contributed review
  ELSEIF rtype=3 THEN RETURN 0.1e0*POW(2e0,s-2e0);  -- with creditable comment
  ELSE RETURN 0.01e0;
  END IF;
END//

CREATE FUNCTION Fac_rawreview (invited TINYINT, s DOUBLE) -- for raw ECP of reviews: a(sinh(b(<s>-c))+d)
RETURNS DOUBLE DETERMINISTIC
BEGIN
  DECLARE x DOUBLE;
  SET x = EXP(1.6e0*(s-1.5e0));
  SET x = (x-1e0/x)*0.5e0 + 0.888e0;
  RETURN IF(invited, 0.6e0*x, 0.4e0*x);
END//
DELIMITER ; -- Revert delimiter back to ';'

CREATE FUNCTION Fac_rawrating (delS DOUBLE) -- raw ECP of a rating
RETURNS DOUBLE DETERMINISTIC
RETURN 1e0-2e0/3e0*ABS(delS);

DELIMITER // -- Temporarily change delimiter to allow ';' inside the procedure body
CREATE PROCEDURE UpdateComments1 ()  -- isolate the update table
BEGIN
    CREATE TABLE UpdateCommentList1 SELECT UpdateCommentList.cID, dID, ctype FROM UpdateCommentList INNER JOIN Comments ON UpdateCommentList.cID = Comments.cID ORDER BY dID;
    TRUNCATE TABLE UpdateCommentList;
    DELETE FROM UpdateCommentList1 WHERE (dID) IN (SELECT dID FROM UpdateDocList);  -- docs will be done later
    CREATE TABLE UpdateRatingList1 (rID BIGINT UNSIGNED,mID BIGINT UNSIGNED,delS DOUBLE,Nth INT UNSIGNED,T INT UNSIGNED); -- create an empty rating list table
END//

CREATE PROCEDURE UpdateComments2 ()  -- do the actual update on comment ECP
BEGIN
    DECLARE dID0,dID1,cID1,rID1,mID1 BIGINT UNSIGNED default 0;
    DECLARE Tresp1 SMALLINT UNSIGNED;
    DECLARE S_ave1,Srev1,delS1,Smax1,Sp1,ecp1,fa1,fp1 DOUBLE;
    DECLARE dtype1,ctype1,rtype1,ano1 TINYINT UNSIGNED;
    DECLARE invited1, Srate1 TINYINT;
    DECLARE t1,n1,nth1,nthr1,nthr2,nthr3 INT UNSIGNED;
    DECLARE done TINYINT DEFAULT FALSE;
    DECLARE cur1 CURSOR FOR SELECT cID, dID, ctype FROM UpdateCommentList1;
    DECLARE cur2 CURSOR FOR SELECT rID, mID, delS, Nth, T FROM UpdateRatingList1;
    DECLARE CONTINUE HANDLER FOR NOT FOUND SET done = TRUE;
    OPEN cur1;
    comm_loop: LOOP
      FETCH cur1 INTO cID1, dID1, ctype1;
      IF done THEN LEAVE comm_loop;
      END IF;
      IF dID1 != dID0 THEN  -- starting comments under a new doc
        IF EXISTS (SELECT 1 FROM UpdateRatingList1) THEN  -- update ratings from the last doc
          OPEN cur2;
          rating_loop: LOOP
            FETCH cur2 INTO rID1,mID1,delS1,nth1,t1;
            IF done THEN LEAVE rating_loop;
            END IF;
            SET Srev1 = NULL;
            SELECT srev,invited INTO Srev1,invited1 FROM SrevList WHERE dID = dID0 AND mID = mID1;
            SET Smax1 = (SELECT smax FROM SmaxList WHERE dID = dID0 AND mID = mID1 ORDER BY smax DESC LIMIT 1);
            SET ecp1 = fa1 * Fac_febr(5,nth1) * Fac_ft(Tresp1, t1) * fp1 * Fac_rawrating(delS1);
            IF ISNULL(Srev1) THEN
              IF ISNULL(Smax1) THEN SET ecp1 = ecp1 * Fac_fd(4,0); -- rating with no creditable contribution
              ELSE SET ecp1 = ecp1 * Fac_fd(3, Smax1);  -- rating with creditable comments
              END IF;
            ELSEIF invited1=1 THEN SET ecp1 = ecp1 * Fac_fd(1, Srev1);  -- rating with invited review
            ELSE SET ecp1 = ecp1 * Fac_fd(2, Srev1);   -- rating with contributed review
            END IF;
            UPDATE CommentRatings SET ECP = ecp1 WHERE rID=rID1; -- update ECP of the rating
          END LOOP rating_loop;
          SET done = FALSE;
          CLOSE cur2;
          TRUNCATE TABLE UpdateRatingList1;  -- empty the rating list
        END IF;
        SET dID0 = dID1;
        SELECT dtype, Tresp, S_ave INTO dtype1, Tresp1, Sp1 FROM Documents WHERE dID = dID1;
        SET fa1 = Fac_fa(Sp1);
        SELECT f_p INTO fp1 FROM DocTypes WHERE ID = dtype1;
      END IF;
      SELECT anonymity, Nth, T, S_ave INTO ano1, nth1, t1, S_ave1 FROM CRComments WHERE cID = cID1;
      CASE ctype1 WHEN 1 THEN SET ecp1 = fa1 * Fac_febc(6, nth1) * Fac_ft(Tresp1, t1) * fp1 * Fac_rawreview(1, S_ave1);  -- invited review
      WHEN 2 THEN SET ecp1 = fa1 * Fac_febc(10, nth1) * Fac_ft(Tresp1, t1) * fp1 * Fac_rawreview(0, S_ave1);  -- contributed review
      WHEN 3 THEN SET ecp1 = Fac_fo(ano1) * Fac_febc(50, nth1) * Fac_ft(Tresp1, t1) * fp1 * (S_ave1-0.5e0);  -- creditable comment
      END CASE;
      UPDATE CRComments SET ECP = ecp1 WHERE cID=cID1;   -- update ECP of comment/review
      -- update Srev/Smax first
      IF ctype1=1 THEN
        INSERT INTO SrevList (dID,mID,srev,invited) SELECT dID1,mID,AL_alloc(S_ave1,duty),1 FROM CommentAuthors WHERE cID=cID1 ON DUPLICATE KEY UPDATE srev=VALUES(srev), invited=VALUES(invited);
      ELSEIF ctype1=2 THEN
        INSERT INTO SrevList (dID,mID,srev,invited) SELECT dID1,mID,AL_alloc(S_ave1,duty),0 FROM CommentAuthors WHERE cID=cID1 ON DUPLICATE KEY UPDATE srev=VALUES(srev), invited=VALUES(invited);
      ELSEIF ctype1=3 THEN
        INSERT INTO SmaxList (dID,mID,cID,smax) SELECT dID1,mID,cID,AL_alloc(S_ave1,duty) FROM CommentAuthors WHERE cID=cID1 ON DUPLICATE KEY UPDATE smax=VALUES(smax);
      END IF;
      -- save the rating list to update their ECP
      INSERT INTO UpdateRatingList1 (rID,mID,delS,Nth,T) SELECT rID,rater_ID,CAST(score AS DOUBLE)/10e0-S_ave1,Nth,T FROM CommentRatings WHERE cID = cID1;
    END LOOP comm_loop;
    CLOSE cur1;
    DROP TABLE IF EXISTS UpdateCommentList1;
    DROP TABLE IF EXISTS UpdateRatingList1;
END//

CREATE PROCEDURE UpdateComments ()  -- first do update on comments then do docs
BEGIN
    CALL UpdateComments1;
    CALL UpdateComments2;
END//
DELIMITER ; -- Revert delimiter back to ';'
